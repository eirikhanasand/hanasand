import { describe, expect, test } from "bun:test";
import { relationshipConfidence } from "../export/relationships.ts";
import { exportEvidenceBackedStixBundle, exportPipelineResultToStixBundle } from "../export/stix.ts";
import { validateStixBundle } from "../export/stixValidation.ts";
import { processCollectedItem } from "../pipeline/pipeline.ts";
import type { RawCapture } from "../types.ts";
import { hashContent } from "../utils.ts";
import { actorIdentity } from "./apiTestHarness.ts";

const actorIdentities = [actorIdentity("G0016", "APT29", ["Nobelium", "Cozy Bear", "Midnight Blizzard"])];

const generatedAt = "2026-05-24T10:05:00.000Z";  function fixture() {   const rawText = [     "APT29 used phishing and credential dumping against Northwind Health Systems.",     "The campaign used Cobalt Strike from https://evil.example.com, 198.51.100.42, and CVE-2025-12345."   ].join(" ");   return processCollectedItem({     sourceId: "src_report",     url: "https://example.test/apt29-report",     collectedAt: "2026-05-24T10:00:00.000Z",     title: "APT29 report",     rawText,     contentHash: hashContent(rawText),     links: [],     metadata: {},     sensitive: false   }, { actorIdentities }); }  describe("compact exchange exports", () => {   test("exports valid STIX-like actor, indicator, TTP, report, and relationship objects", () => {     const bundle = exportPipelineResultToStixBundle(fixture(), { producerName: "ti-scraper", generatedAt });      expect(validateStixBundle(bundle).valid).toBe(true);     expect(bundle.objects.some((object) => object.type === "intrusion-set" && object.name === "APT29")).toBe(true);     expect(bundle.objects.some((object) => object.type === "indicator" && object.name?.includes("198.51.100.42"))).toBe(true);     expect(bundle.objects.some((object) => object.type === "vulnerability" && object.name === "CVE-2025-12345")).toBe(true);     expect(bundle.objects.some((object) => object.type === "attack-pattern")).toBe(true);     expect(bundle.objects.some((object) => object.type === "relationship" && object.x_ti_provenance?.length)).toBe(true);   });    test("exports stored captures without leaking unavailable bodies", () => {     const body = "APT29 used phishing and Cobalt Strike against Northwind Health Systems from 198.51.100.42.";     const capture: RawCapture = {       id: "cap_live_stix",       sourceId: "src_live",       url: "https://example.test/live-report",       collectedAt: "2026-05-24T10:00:00.000Z",       contentHash: hashContent(body),       mediaType: "text/plain",       storageKind: "inline_text",       body,       metadata: { title: "Live APT29 report" },       sensitive: false     };     const metadataOnly: RawCapture = {       id: "cap_metadata_only",       sourceId: "src_public_channel",       url: "https://t.me/public_channel/12",       collectedAt: "2026-05-24T10:00:00.000Z",       contentHash: "sha256:metadata",       mediaType: "application/json",       storageKind: "metadata_only",       metadata: { title: "Public channel metadata" },       sensitive: true     };      const bundle = exportEvidenceBackedStixBundle({ captures: [capture, metadataOnly], options: { producerName: "ti-scraper", generatedAt } });      expect(validateStixBundle(bundle).valid).toBe(true);     expect(bundle.objects.some((object) => object.type === "x-ti-evidence" && object.x_ti_extractable === false)).toBe(true);     expect(JSON.stringify(bundle)).not.toContain("body");     expect(bundle.objects.every((object) => object.x_ti_provenance ? object.x_ti_provenance.every((item) => item.captureId === "cap_live_stix") : true)).toBe(true);   });    test("keeps confidence rules conservative", () => {     expect(relationshipConfidence("actor-targets-victim", 0.8, 0.6)).toBeCloseTo(0.574, 3);     expect(relationshipConfidence("actor-uses-malware", 0.8, 0.6)).toBeCloseTo(0.616, 3);   }); });
