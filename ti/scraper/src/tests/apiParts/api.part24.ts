import { describe, expect, test, mkdtempSync, rmSync, join, tmpdir, handleApiRequest, startApiServer, loadRuntimeConfig, FocusedFrontier, activatePublicCanarySources, buildCanaryOperatorSummary, runCanaryCollectionCycle, startCanaryCollectionLoop, createLogger, MetricsRegistry, WorkerSupervisor, processCollectedItem, FileBackedScraperStore, InMemoryObjectEvidenceStore, InMemoryScraperStore, hashContent, api, apiRestrictedMetadataApplyPlanSources, body, fixtureCapture, fixtureDelta, restrictedMetadataApplyPlanSources, seedEvidenceReplayFixture, source, telegramCapture } from "../apiTestHarness.ts";
import type { AnalystClaimLedgerEntry, CanaryOperatorResponseForTest, CanaryReadinessResponseForTest, CanarySoakResponseForTest, RawCapture, SourceRecord } from "../apiTestHarness.ts";

describe("api v1", () => {   test("reports partial public-channel evidence for live intel search", async () => {     const store = new InMemoryScraperStore();     const telegram = source({       id: "src_telegram",       name: "Cybercrime public channel",       type: "telegram_public",       url: "https://t.me/cybercrimeintel",       accessMethod: "official_api",       status: "active",       risk: "medium",       legalNotes: "Reviewed public Telegram channel fixture.",       approvedAt: new Date(0).toISOString(),       approvedBy: "reviewer",       governance: {         approvalRequired: true,         approvalState: "approved",         metadataOnly: false,         approvedAt: new Date(0).toISOString(),         approvedBy: "reviewer"       },       metadata: { actors: ["UNC3944"], topicTags: ["cybercrime"] }     });     store.saveSource(telegram);     const bodyText = "Scattered Spider posted infrastructure at https://evil.example";     const capture: RawCapture = {       id: "cap_telegram",       sourceId: "src_telegram",       url: "https://t.me/cybercrimeintel/10",       collectedAt: "2026-05-24T00:00:00.000Z",       publishedAt: "2026-05-23T23:59:00.000Z",       contentHash: hashContent(bodyText),       mediaType: "text/plain",       storageKind: "inline_text",       body: bodyText,       metadata: {         adapter: "telegram_public",         channel: "cybercrimeintel",         messageId: 10,         messageState: "available",         urlMentions: ["https://evil.example"],         forward: { fromChannel: "public_origin", fromMessageId: 4 },         provenance: { confidence: 0.95 }       },       sensitive: false     };     store.saveCapture(capture);      const response = await body(await handleApiRequest(api("/v1/intel/search?q=Scattered%20Spider&entityType=actor"), {       store,       frontier: new FocusedFrontier()     }));      const publicChannel = response.publicChannel as {       queuedTasks: number;       sla: { metrics: { ledgerBackedClaimYield: { ledgerBackedClaimCount: number; candidateClaimCount: number } } };       cutoverReport: { evidenceFreshness: { latestMessageId: number; safePartialEvidenceCount: number } };       canaryRollout: { mode: string; summary: { selectedSourceCount: number; replayableEvidenceCount: number }; selectedSources: Array<{ sourceId: string; agent06EvidenceHandoff: { metadataOnly: boolean } }> };       promotionCanary: { mode: string; summary: { evidenceCount: number; noLeakSerialization: boolean }; handoffs: { agent06EvidenceCutover: { evidenceCutoverReady: boolean } } };       promotionCertification: { mode: string; summary: { noLeakSerialization: boolean } };       evidence: Array<{ sourceId: string; channel: string; messageUrl: string; extractedUrls: string[]; confidence: number }>;       abuseControls: Array<{ sourceId: string; allowed: boolean }>;     };     expect(publicChannel.queuedTasks).toBe(1);     expect(publicChannel.sla.metrics.ledgerBackedClaimYield).toMatchObject({ ledgerBackedClaimCount: 1, candidateClaimCount: 1 });     expect(publicChannel.cutoverReport.evidenceFreshness).toMatchObject({ latestMessageId: 10, safePartialEvidenceCount: 1 });     expect(publicChannel.canaryRollout.mode).toBe("dry_run");     expect(publicChannel.canaryRollout.summary).toMatchObject({ selectedSourceCount: 1, replayableEvidenceCount: 1 });     expect(publicChannel.canaryRollout.selectedSources).toEqual(expect.arrayContaining([       expect.objectContaining({ sourceId: "src_telegram", agent06EvidenceHandoff: expect.objectContaining({ metadataOnly: true }) })     ]));     expect(publicChannel.promotionCanary.mode).toBe("dry_run");     expect(publicChannel.promotionCanary.summary).toMatchObject({ evidenceCount: 1, noLeakSerialization: true });     expect(publicChannel.promotionCanary.handoffs.agent06EvidenceCutover.evidenceCutoverReady).toBe(true);     expect(publicChannel.promotionCertification).toMatchObject({ mode: "dry_run", summary: { noLeakSerialization: true } });     expect(publicChannel.abuseControls).toEqual(expect.arrayContaining([expect.objectContaining({ sourceId: "src_telegram", allowed: true })]));     expect(publicChannel.evidence).toEqual(expect.arrayContaining([expect.objectContaining({       sourceId: "src_telegram",       channel: "cybercrimeintel",       messageUrl: "https://t.me/cybercrimeintel/10",       extractedUrls: ["https://evil.example"],       confidence: 0.95     })]));     expect(response.planner).toMatchObject({       mode: "interactive_live_search",       zeroTaskReason: "none"     });     expect((response.planner as { queuedTaskCount: number }).queuedTaskCount).toBeGreaterThan(0);     expect(response.publicTiAnswer).toMatchObject({       publicChannelCertification: {         status: expect.any(String),         summary: expect.objectContaining({           noLeakSerialization: true         }),         answerStateMachine: expect.objectContaining({ state: expect.any(String) }),         graphCertification: expect.objectContaining({ status: expect.any(String) }),         rcGate: expect.objectContaining({ decision: expect.any(String) })       }     });   }); });
