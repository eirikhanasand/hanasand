import { describe, expect, test, mkdtempSync, rmSync, join, tmpdir, handleApiRequest, startApiServer, loadRuntimeConfig, FocusedFrontier, activatePublicCanarySources, buildCanaryOperatorSummary, runCanaryCollectionCycle, startCanaryCollectionLoop, processCollectedItem, FileBackedScraperStore, InMemoryObjectEvidenceStore, InMemoryScraperStore, hashContent, api, apiRestrictedMetadataApplyPlanSources, body, fixtureCapture, fixtureDelta, restrictedMetadataApplyPlanSources, seedEvidenceReplayFixture, source, telegramCapture } from "../apiTestHarness.ts";
import type { AnalystClaimLedgerEntry, CanaryOperatorResponseForTest, CanaryReadinessResponseForTest, CanarySoakResponseForTest, RawCapture, SourceRecord } from "../apiTestHarness.ts";

describe("api v1", () => {   test("returns frozen source apply-plan contract without mutating sources or crawling", async () => {     const store = new InMemoryScraperStore();     const reviewedAt = new Date().toISOString();     const candidate = source({ id: "src_source_apply_candidate", status: "candidate", tenantId: "tenant_source_apply", tags: ["apt29"], url: "https://candidate.example.test/feed", metadata: { legalNotesReviewedAt: reviewedAt } });     const unhealthy = source({       id: "src_source_apply_unhealthy",       status: "active",       tenantId: "tenant_source_apply",       tags: ["apt29"],       url: "https://unhealthy.example.test/feed",       metadata: { legalNotesReviewedAt: reviewedAt },       health: { status: "failing", consecutiveFailures: 5, errorRate: 0.9 }     });     const duplicateA = source({ id: "src_source_apply_duplicate_a", url: "https://duplicate.example.test/feed", tenantId: "tenant_source_apply", tags: ["apt29"], metadata: { legalNotesReviewedAt: reviewedAt } });     const duplicateB = source({ id: "src_source_apply_duplicate_b", url: "https://duplicate.example.test/feed", tenantId: "tenant_source_apply", tags: ["apt29"], metadata: { legalNotesReviewedAt: reviewedAt } });     for (const item of [candidate, unhealthy, duplicateA, duplicateB]) store.saveSource(item);     const before = store.listSources().map((item) => `${item.id}:${item.status}`).sort();      const response = await body(await handleApiRequest(api("/v1/sources/apply-plan", {       method: "POST",       headers: { "content-type": "application/json", "x-tenant-id": "tenant_source_apply" },       body: JSON.stringify({         queryScope: { queries: ["APT29"], entityTypes: ["actor"] },         sourcePackIds: ["safe-public-cti-starter-pack"],         selectedActions: ["approve", "quarantine", "retire", "request_legal_notes", "leave_unchanged"],         includeExecutionPreview: true       })     }), { store, frontier: new FocusedFrontier() }));     const after = store.listSources().map((item) => `${item.id}:${item.status}`).sort();     const contract = response.contract as {       endpoint: string;       mode: string;       response: { actions: string[]; forbiddenMutationFields: string[] };       examples: Array<{ name: string }>;     };     const applyPlan = response.applyPlan as {       endpoint: string;       dryRun: boolean;       willMutate: boolean;       willStartCrawling: boolean;       approvalSummary: { approvalsRequired: number; rollbackOnly: number };       items: Array<{ action: string; collectionImpact: { willStartCrawling: boolean }; automation: string }>;       executionPreview: { dryRun: boolean; executed: boolean; itemResults: Array<{ reason: string }> };       promotionPacketLink: { field: string };       schemaExamples: Array<{ name: string; response: { willMutate: boolean; willStartCrawling: boolean } }>;     };      expect(contract.endpoint).toBe("/v1/sources/apply-plan");     expect(contract.mode).toBe("dry_run");     expect(contract.response.actions).toEqual(expect.arrayContaining(["approve", "quarantine", "retire", "request_legal_notes"]));     expect(contract.response.forbiddenMutationFields).toEqual(expect.arrayContaining(["updatedSource", "startedCrawl", "restrictedActivation"]));     expect(contract.examples.map((example) => example.name)).toEqual(expect.arrayContaining([       "happy_path",       "human_approval_required",       "blocked_restricted_source",       "duplicate_source",       "stale_legal_notes",       "rollback_only_quarantine"     ]));     expect(applyPlan.endpoint).toBe("/v1/sources/apply-plan");     expect(applyPlan.dryRun).toBe(true);     expect(applyPlan.willMutate).toBe(false);     expect(applyPlan.willStartCrawling).toBe(false);     expect(applyPlan.approvalSummary.approvalsRequired).toBeGreaterThanOrEqual(0);     expect(applyPlan.approvalSummary.rollbackOnly).toBeGreaterThan(0);     expect(applyPlan.items.every((item) => item.collectionImpact.willStartCrawling === false)).toBe(true);     expect(applyPlan.items.map((item) => item.action)).toEqual(expect.arrayContaining(["approve", "quarantine"]));     expect(applyPlan.executionPreview.dryRun).toBe(true);     expect(applyPlan.executionPreview.executed).toBe(false);     expect(applyPlan.executionPreview.itemResults.every((item) => item.reason.includes("Dry-run") || item.reason.includes("blocked"))).toBe(true);     expect(applyPlan.promotionPacketLink.field).toBe("sourceApplyPlanId");     expect(applyPlan.schemaExamples.every((example) => example.response.willMutate === false && example.response.willStartCrawling === false)).toBe(true);     expect(after).toEqual(before);   }); });
