import { describe, expect, test, mkdtempSync, rmSync, join, tmpdir, handleApiRequest, startApiServer, loadRuntimeConfig, FocusedFrontier, activatePublicCanarySources, buildCanaryOperatorSummary, runCanaryCollectionCycle, startCanaryCollectionLoop, processCollectedItem, FileBackedScraperStore, InMemoryObjectEvidenceStore, InMemoryScraperStore, hashContent, api, apiRestrictedMetadataApplyPlanSources, body, fixtureCapture, fixtureDelta, restrictedMetadataApplyPlanSources, seedEvidenceReplayFixture, source, telegramCapture } from "../apiTestHarness.ts";
import type { AnalystClaimLedgerEntry, CanaryOperatorResponseForTest, CanaryReadinessResponseForTest, CanarySoakResponseForTest, RawCapture, SourceRecord } from "../apiTestHarness.ts";

describe("api v1", () => {   test("returns frozen public-channel apply-plan contract without raw payload fields", async () => {     const store = new InMemoryScraperStore();     store.saveSource(source({       id: "unsafe_private",       name: "Unsafe private Telegram",       type: "telegram_public",       url: "https://t.me/+privateInvite",       accessMethod: "official_api",       status: "candidate",       risk: "medium",       legalNotes: "Unsafe fixture should never activate.",       governance: {         approvalRequired: true,         approvalState: "pending",         metadataOnly: false       },       metadata: {         actors: ["APT29"],         accountAutomation: true,         rawMessage: "do not expose",         mediaPayload: "do not expose"       }     }));      const response = await body(await handleApiRequest(api("/v1/public-channels/apply-plan", {       method: "POST",       headers: { "content-type": "application/json" },       body: JSON.stringify({ query: "APT29", entityType: "actor" })     }), {       store,       frontier: new FocusedFrontier()     }));      expect(response.contract).toMatchObject({       endpoint: "/v1/public-channels/apply-plan",       method: "POST",       mode: "dry_run",       examples: {         automationSafe: { execution: "automation_safe" },         humanApprovalRequired: { execution: "human_approval_required" },         blockedPrivateTarget: { execution: "blocked" },         rateLimitedChannel: { action: "delay_poll" },         rollbackOnlyQuarantine: { execution: "rollback_only" }       }     });     const applyPlan = response.applyPlan as {       steps: Array<{ action: string; execution: string; prerequisites: string[] }>;       promotionGate: { metadataOnlyMedia: boolean; piiMinimizationRequired: boolean };     };     expect(applyPlan.steps.some((step) => step.action === "activate_source_pack")).toBe(false);     expect(applyPlan.steps.find((step) => step.action === "request_review")).toMatchObject({       execution: "blocked",       prerequisites: expect.arrayContaining(["blocked: private, invite, or account-automation targets cannot be activated"])     });     expect(applyPlan.promotionGate).toMatchObject({       metadataOnlyMedia: true,       piiMinimizationRequired: true     });     const serializedApplyPlan = JSON.stringify(response.applyPlan);     expect(serializedApplyPlan).not.toContain("do not expose");     expect(serializedApplyPlan).not.toContain("rawMessage");     expect(serializedApplyPlan).not.toContain("mediaPayload");     expect(serializedApplyPlan).not.toContain("+privateInvite");   }); });
