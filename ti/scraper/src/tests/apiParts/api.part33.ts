import { describe, expect, test, mkdtempSync, rmSync, join, tmpdir, handleApiRequest, startApiServer, loadRuntimeConfig, FocusedFrontier, activatePublicCanarySources, buildCanaryOperatorSummary, runCanaryCollectionCycle, startCanaryCollectionLoop, createLogger, MetricsRegistry, WorkerSupervisor, processCollectedItem, FileBackedScraperStore, InMemoryObjectEvidenceStore, InMemoryScraperStore, hashContent, api, apiRestrictedMetadataApplyPlanSources, body, fixtureCapture, fixtureDelta, restrictedMetadataApplyPlanSources, seedEvidenceReplayFixture, source, telegramCapture } from "../apiTestHarness.ts";
import type { AnalystClaimLedgerEntry, CanaryOperatorResponseForTest, CanaryReadinessResponseForTest, CanarySoakResponseForTest, RawCapture, SourceRecord } from "../apiTestHarness.ts";

describe("api v1", () => {   test("mounted public-channel apply-plan endpoint handles proof cases without leaking unsafe fields", async () => {     const pack = await Bun.file("seeds/public_telegram_channel_packs.json").json();     const store = new InMemoryScraperStore();     store.saveSource(source({       id: "src_public_review_mounted",       name: "Mounted pending public channel",       type: "telegram_public",       url: "https://t.me/public_review_mounted",       accessMethod: "official_api",       status: "candidate",       risk: "medium",       legalNotes: "Pending public Telegram review.",       governance: { approvalRequired: true, approvalState: "pending", metadataOnly: false },       metadata: { actors: ["APT29"] }     }));     store.saveSource(source({       id: "src_public_rate_limited_mounted",       name: "Mounted rate-limited public channel",       type: "telegram_public",       url: "https://t.me/public_rate_limited_mounted",       accessMethod: "official_api",       status: "active",       risk: "medium",       legalNotes: "Approved public Telegram channel.",       approvedAt: "2026-01-01T00:00:00.000Z",       approvedBy: "reviewer",       governance: { approvalRequired: true, approvalState: "approved", metadataOnly: false, approvedAt: "2026-01-01T00:00:00.000Z", approvedBy: "reviewer" },       metadata: { actors: ["APT29"], rateLimitResetAt: "2999-01-01T00:00:00.000Z" }     }));     store.saveSource(source({       id: "src_public_private_mounted",       name: "Mounted unsafe private channel",       type: "telegram_public",       url: "https://t.me/+privateInvite",       accessMethod: "official_api",       status: "active",       risk: "medium",       legalNotes: "Unsafe mounted fixture.",       approvedAt: "2026-01-01T00:00:00.000Z",       approvedBy: "reviewer",       governance: { approvalRequired: true, approvalState: "approved", metadataOnly: false, approvedAt: "2026-01-01T00:00:00.000Z", approvedBy: "reviewer" },       metadata: {         actors: ["APT29"],         accountAutomation: true,         rawMessage: "mounted raw body must not leak",         mediaPayload: "mounted media payload must not leak",         sessionString: "mounted session must not leak"       }     }));      const server = startApiServer({       port: 0,       store,       frontier: new FocusedFrontier(),       publicTelegramSourcePacks: [pack]     });     const post = async (payload: Record<string, unknown>) => {       const response = await fetch(`http://127.0.0.1:${server.port}/v1/public-channels/apply-plan`, {         method: "POST",         headers: { "content-type": "application/json" },         body: JSON.stringify(payload)       });       return { response, payload: await response.json() as Record<string, unknown> };     };      try {       const activation = await post({ query: "Volt Typhoon", entityType: "actor", actions: ["activate_source_pack"] });       expect(activation.response.status).toBe(200);       expect(activation.payload.applyPlan).toMatchObject({         summary: { humanApprovalRequiredCount: 3 },         steps: expect.arrayContaining([           expect.objectContaining({ action: "activate_source_pack", execution: "human_approval_required" })         ])       });        const review = await post({ query: "APT29", entityType: "actor", actions: ["request_review"] });       expect(review.payload.applyPlan).toMatchObject({         steps: expect.arrayContaining([           expect.objectContaining({ action: "request_review", execution: "human_approval_required" }),           expect.objectContaining({ action: "request_review", execution: "blocked" })         ])       });        const rateLimited = await post({ query: "APT29", entityType: "actor", actions: ["delay_poll"] });       expect(rateLimited.payload.applyPlan).toMatchObject({         summary: { automationSafeCount: 1, canAutoApply: true },         steps: [expect.objectContaining({ action: "delay_poll", execution: "automation_safe" })]       });        const rollback = await post({ query: "APT29", entityType: "actor", actions: ["quarantine_channel"] });       expect(rollback.payload.applyPlan).toMatchObject({         summary: { rollbackOnlyCount: 1 },         steps: [expect.objectContaining({ action: "quarantine_channel", execution: "rollback_only" })]       });        const statusResponse = await fetch(`http://127.0.0.1:${server.port}/v1/public-channels/status?q=APT29&entityType=actor`);       const statusPayload = await statusResponse.json() as Record<string, unknown>;       expect(statusPayload).toMatchObject({         endpoint: "/v1/public-channels/status",         operatorStates: expect.arrayContaining([           expect.objectContaining({ sourceId: "src_public_review_mounted", state: "pending_review" }),           expect.objectContaining({ sourceId: "src_public_rate_limited_mounted", state: "delayed" }),           expect.objectContaining({ sourceId: "src_public_private_mounted", state: "policy_blocked" })         ]),         sourcePackCompatibility: expect.any(Array),         sourcePackReadiness: expect.objectContaining({           summary: expect.objectContaining({             sourcePackCount: expect.any(Number),             candidateCount: expect.any(Number)           }),           safeOutput: expect.objectContaining({             rawPrivateDataExposed: false,             rawMediaPayloadsExposed: false,             credentialsExposed: false           })         }),         actorReadiness: {           status: expect.any(String)         },         answerReadiness: {           reliability: {             sourceCount: expect.any(Number)           },           promotionYield: {             rating: expect.any(String)           }         },         operatorControlEffects: expect.arrayContaining([           expect.objectContaining({ action: "request_review", expectedAnswerQualityEffect: "queues_human_review_for_readiness" }),           expect.objectContaining({ action: "delay_poll", expectedAnswerQualityEffect: "delays_freshness_keeps_claims_partial" })         ])       });        const invalid = await post({ query: "APT29", actions: ["join_private_group"] });       expect(invalid.response.status).toBe(400);       expect(invalid.payload).toMatchObject({         error: {           code: "invalid_action",           details: { invalidActions: ["join_private_group"] }         }       });        const serialized = JSON.stringify({ activation, review, rateLimited, rollback, statusPayload, invalid });       expect(serialized).not.toContain("mounted raw body must not leak");       expect(serialized).not.toContain("mounted media payload must not leak");       expect(serialized).not.toContain("mounted session must not leak");       expect(serialized).not.toContain("+privateInvite");     } finally {       await server.stop();     }   }); });
