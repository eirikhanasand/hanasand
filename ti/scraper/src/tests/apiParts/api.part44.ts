import { describe, expect, test, mkdtempSync, rmSync, join, tmpdir, handleApiRequest, startApiServer, loadRuntimeConfig, FocusedFrontier, activatePublicCanarySources, buildCanaryOperatorSummary, runCanaryCollectionCycle, startCanaryCollectionLoop, createLogger, MetricsRegistry, WorkerSupervisor, processCollectedItem, FileBackedScraperStore, InMemoryObjectEvidenceStore, InMemoryScraperStore, hashContent, api, apiRestrictedMetadataApplyPlanSources, body, fixtureCapture, fixtureDelta, restrictedMetadataApplyPlanSources, seedEvidenceReplayFixture, source, telegramCapture } from "../apiTestHarness.ts";
import type { AnalystClaimLedgerEntry, CanaryOperatorResponseForTest, CanaryReadinessResponseForTest, CanarySoakResponseForTest, RawCapture, SourceRecord } from "../apiTestHarness.ts";

describe("api v1", () => {   test("smokes mounted source apply-plan endpoint for happy restricted and invalid proof cases", async () => {     const reviewedAt = new Date().toISOString();     const scenarios: Array<{       name: "happy_path" | "blocked_restricted_source" | "invalid_action";       sources: SourceRecord[];       request: Record<string, unknown>;       headers?: Record<string, string>;       status: number;       expectedError?: string;     }> = [       {         name: "happy_path",         sources: [           source({ id: "src_mounted_source_candidate", status: "candidate", tenantId: "tenant_mounted_source", tags: ["apt29"], url: "https://candidate-mounted.example.test/feed", metadata: { legalNotesReviewedAt: reviewedAt } }),           source({ id: "src_mounted_source_unhealthy", status: "active", tenantId: "tenant_mounted_source", tags: ["apt29"], url: "https://unhealthy-mounted.example.test/feed", metadata: { legalNotesReviewedAt: reviewedAt }, health: { status: "failing", consecutiveFailures: 5, errorRate: 0.9 } }),           source({ id: "src_mounted_source_duplicate_a", tenantId: "tenant_mounted_source", tags: ["apt29"], url: "https://duplicate-mounted.example.test/feed", metadata: { legalNotesReviewedAt: reviewedAt } }),           source({ id: "src_mounted_source_duplicate_b", tenantId: "tenant_mounted_source", tags: ["apt29"], url: "https://duplicate-mounted.example.test/feed", metadata: { legalNotesReviewedAt: reviewedAt } })         ],         request: {           queryScope: { queries: ["APT29"], entityTypes: ["actor"] },           sourcePackIds: ["safe-public-cti-starter-pack"],           selectedActions: ["approve", "quarantine", "retire", "request_legal_notes", "leave_unchanged"],           includeExecutionPreview: true         },         headers: { "x-tenant-id": "tenant_mounted_source" },         status: 200       },       {         name: "blocked_restricted_source",         sources: [           source({             id: "src_mounted_source_restricted",             name: "Mounted Restricted Source",             type: "tor_metadata",             url: "http://restricted-mounted.onion",             accessMethod: "approved_proxy",             risk: "restricted",             status: "approved",             tenantId: "tenant_mounted_restricted",             governance: {               approvalRequired: true,               approvalState: "approved",               metadataOnly: true,               approvedAt: "2026-05-24T00:00:00.000Z",               approvedBy: "legal",               policyVersion: "collection-policy:v1"             },             metadata: { legalNotesReviewedAt: reviewedAt },             tags: ["apt29"]           })         ],         request: {           queryScope: { queries: ["APT29"], entityTypes: ["actor"] },           selectedActions: ["activate", "quarantine", "request_legal_notes"],           includeExecutionPreview: true         },         headers: { "x-tenant-id": "tenant_mounted_restricted" },         status: 200       },       {         name: "invalid_action",         sources: [source({ id: "src_mounted_source_invalid", tenantId: "tenant_mounted_invalid", tags: ["apt29"] })],         request: {           queryScope: { queries: ["APT29"], entityTypes: ["actor"] },           selectedActions: ["launch_crawler"]         },         headers: { "x-tenant-id": "tenant_mounted_invalid" },         status: 400,         expectedError: "invalid_action"       }     ];      for (const scenario of scenarios) {       const store = new InMemoryScraperStore();       for (const item of scenario.sources) store.saveSource(item);       const frontier = new FocusedFrontier();       const beforeSources = store.listSources().map((item) => `${item.id}:${item.status}:${item.updatedAt}`).sort();       const beforeQueued = frontier.snapshot().map((item) => item.task.id).sort();       const beforeLeased = frontier.leasedSnapshot().map((task) => task.id).sort();       const server = startApiServer({ port: 0, store, frontier });       try {         const response = await fetch(`http://127.0.0.1:${server.port}/v1/sources/apply-plan`, {           method: "POST",           headers: { "content-type": "application/json", ...(scenario.headers ?? {}) },           body: JSON.stringify(scenario.request)         });         const payload = await response.json() as {           contract?: { endpoint: string; response: { forbiddenMutationFields: string[] } };           applyPlan?: {             dryRun: boolean;             willMutate: boolean;             willStartCrawling: boolean;             items: Array<{               action: string;               automation: string;               collectionImpact: { willStartCrawling: boolean; enablesCollection: boolean; remainsDisabled: string[] };             }>;             executionPreview?: { dryRun: boolean; executed: boolean; itemResults: Array<{ wouldApply: boolean }> };           };           error?: { code: string };         };         const serialized = JSON.stringify(payload);          expect(response.status).toBe(scenario.status);         if (scenario.status === 200) {           expect(payload.contract).toMatchObject({             endpoint: "/v1/sources/apply-plan",             response: {               forbiddenMutationFields: expect.arrayContaining(["updatedSource", "startedCrawl", "dbTransaction", "restrictedActivation"])             }           });           expect(payload.applyPlan).toMatchObject({             dryRun: true,             willMutate: false,             willStartCrawling: false           });           expect(payload.applyPlan?.items.every((item) => item.collectionImpact.willStartCrawling === false)).toBe(true);           expect(payload.applyPlan?.executionPreview?.dryRun).toBe(true);           expect(payload.applyPlan?.executionPreview?.executed).toBe(false);           if (scenario.name === "happy_path") {             expect(payload.applyPlan?.items.map((item) => item.action)).toEqual(expect.arrayContaining(["approve", "quarantine"]));             expect(payload.applyPlan?.items.map((item) => item.automation)).toContain("rollback_only");           }           if (scenario.name === "blocked_restricted_source") {             expect(payload.applyPlan?.items.some((item) => item.action === "activate" && item.collectionImpact.enablesCollection)).toBe(false);             expect(payload.applyPlan?.items.flatMap((item) => item.collectionImpact.remainsDisabled)).toEqual(expect.arrayContaining([               "restricted raw payload collection",               "automatic restricted-source activation"             ]));           }         } else {           expect(payload.error?.code).toBe(scenario.expectedError);         }          const applyPlanSerialized = JSON.stringify(payload.applyPlan ?? {});         expect(applyPlanSerialized).not.toContain("startedCrawl");         expect(applyPlanSerialized).not.toContain("updatedSource");         expect(applyPlanSerialized).not.toContain("reviewDecisionApplied");         expect(applyPlanSerialized).not.toContain("dbTransaction");         expect(applyPlanSerialized).not.toContain("restrictedActivation");       } finally {         await server.stop();       }       expect(store.listSources().map((item) => `${item.id}:${item.status}:${item.updatedAt}`).sort()).toEqual(beforeSources);       expect(frontier.snapshot().map((item) => item.task.id).sort()).toEqual(beforeQueued);       expect(frontier.leasedSnapshot().map((task) => task.id).sort()).toEqual(beforeLeased);     }   }); });
