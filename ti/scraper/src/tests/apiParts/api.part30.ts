import { describe, expect, test, mkdtempSync, rmSync, join, tmpdir, handleApiRequest, startApiServer, loadRuntimeConfig, FocusedFrontier, activatePublicCanarySources, buildCanaryOperatorSummary, runCanaryCollectionCycle, startCanaryCollectionLoop, createLogger, MetricsRegistry, WorkerSupervisor, processCollectedItem, FileBackedScraperStore, InMemoryObjectEvidenceStore, InMemoryScraperStore, hashContent, api, apiRestrictedMetadataApplyPlanSources, body, fixtureCapture, fixtureDelta, restrictedMetadataApplyPlanSources, seedEvidenceReplayFixture, source, telegramCapture } from "../apiTestHarness.ts";
import type { AnalystClaimLedgerEntry, CanaryOperatorResponseForTest, CanaryReadinessResponseForTest, CanarySoakResponseForTest, RawCapture, SourceRecord } from "../apiTestHarness.ts";

describe("api v1", () => {   test("routes public-channel apply-plan source-pack activation and review contracts", async () => {     const pack = await Bun.file("seeds/public_telegram_channel_packs.json").json();     const activation = await body(await handleApiRequest(api("/v1/public-channels/apply-plan", {       method: "POST",       headers: { "content-type": "application/json" },       body: JSON.stringify({ query: "Volt Typhoon", entityType: "actor", actions: ["activate_source_pack"] })     }), {       store: new InMemoryScraperStore(),       frontier: new FocusedFrontier(),       publicTelegramSourcePacks: [pack]     }));      expect(activation.applyPlan).toMatchObject({       summary: {         stepCount: 7,         humanApprovalRequiredCount: 7,         automationSafeCount: 0       }     });     expect((activation.applyPlan as { steps: Array<Record<string, unknown>> }).steps[0]).toMatchObject({       action: "activate_source_pack",       execution: "human_approval_required",       manual: true,       automationSafe: false     });     expect(activation.canaryRollout).toMatchObject({       mode: "dry_run",       status: "hold",       summary: {         selectedSourceCount: 0,         pendingReviewCount: 7,         releaseTrain: "hold"       },       controls: {         rollback: {           dryRunOnly: true,           quarantineSupported: true         }       }     });     expect(activation.promotionCanary).toMatchObject({       mode: "dry_run",       summary: {         evidenceCount: 0,         noLeakSerialization: true       },       handoffs: {         agent06EvidenceCutover: expect.objectContaining({ evidenceCutoverReady: true }),         agent10RcGate: expect.objectContaining({ status: expect.any(String) })       }     });     expect(activation.promotionCertification).toMatchObject({       mode: "dry_run",       summary: {         certifiedEvidenceCount: 0,         sourceHealthUpdateCount: expect.any(Number),         noLeakSerialization: true       },       handoffs: {         agent07AnswerStateMachine: expect.objectContaining({ state: expect.any(String) }),         agent10RcGate: expect.objectContaining({ decision: expect.any(String) })       }     });      const store = new InMemoryScraperStore();     store.saveSource(source({       id: "src_pending_review",       name: "Pending public channel",       type: "telegram_public",       url: "https://t.me/public_review_channel",       accessMethod: "official_api",       status: "candidate",       risk: "medium",       legalNotes: "Pending public Telegram review.",       governance: { approvalRequired: true, approvalState: "pending", metadataOnly: false },       metadata: { actors: ["APT29"] }     }));     const review = await body(await handleApiRequest(api("/v1/public-channels/apply-plan", {       method: "POST",       headers: { "content-type": "application/json" },       body: JSON.stringify({ query: "APT29", entityType: "actor", actions: ["request_review"] })     }), {       store,       frontier: new FocusedFrontier()     }));      expect(review.applyPlan).toMatchObject({       summary: {         stepCount: 1,         humanApprovalRequiredCount: 1       },       steps: [{         action: "request_review",         execution: "human_approval_required"       }]     });   }); });
