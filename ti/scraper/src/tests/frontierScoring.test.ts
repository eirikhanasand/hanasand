import { describe, expect, test } from "bun:test";
import { FocusedFrontier } from "../frontier/frontier.ts";
import type { FrontierStrategy } from "../types.ts";
import { source } from "./helpers/frontierFixtures.ts";

describe("focused frontier scoring", () => {   test("enqueues high-relevance CTI candidates and drops inactive sources", () => {     const frontier = new FocusedFrontier();     const score = frontier.add({ source, url: "https://example.test/ransomware-campaign", discoveredAt: new Date().toISOString(), anchorText: "ransomware campaign indicators", surroundingText: "APT malware CVE exploit details", parentRelevance: 0.9, novelty: 0.8, freshness: 0.8 });     expect(score.decision).toBe("enqueue");     expect(frontier.snapshot()).toHaveLength(1);     const dropped = frontier.add({ source: { ...source, status: "paused" }, url: "https://example.test/ransomware", discoveredAt: new Date().toISOString(), anchorText: "ransomware" });     expect(dropped.decision).toBe("drop");     expect(frontier.snapshot()).toHaveLength(1);   });    test("uses strategy thresholds for precision and recall crawls", () => {     const candidate = { source, url: "https://example.test/vendors/weekly-roundup", discoveredAt: new Date().toISOString(), anchorText: "weekly roundup", surroundingText: "APT29 mentioned in vendor telemetry", parentRelevance: 0.55, novelty: 0.7, freshness: 0.8 };     expect(new FocusedFrontier({ strategy: "recall" }).score(candidate).decision).toBe("enqueue");     expect(new FocusedFrontier({ strategy: "precision" }).score(candidate).decision).toBe("review");   });    test("scores link parent and destination classifiers across strategy modes", () => {     const strategies: FrontierStrategy[] = ["precision", "recall", "balanced", "efficiency", "link_only", "parent_only", "destination_only", "link_parent", "link_destination", "parent_destination", "hybrid_dynamic"];     const candidate = { source, url: "https://example.test/research/cve-2026-9999", discoveredAt: new Date().toISOString(), anchorText: "APT29 CVE-2026-9999 exploit indicators", surroundingText: "malware campaign with IOCs and victim telemetry", parentTitle: "Vendor threat research on APT29", parentText: "Parent page discusses intrusion activity, malware, campaign infrastructure, and TTPs.", parentRelevance: 0.86, destinationTitle: "APT29 exploitation report", destinationText: "Destination confirms CVE-2026-9999 exploitation, indicators, backdoor deployment, and affected government victims.", destinationRelevance: 0.92, novelty: 0.82, freshness: 0.88 };     const scores = strategies.map((strategy) => new FocusedFrontier({ strategy }).score(candidate));     expect(scores).toHaveLength(11);     expect(new Set(scores.map((score) => score.classifier?.selectedStrategy)).size).toBe(11);     for (const score of scores) {       expect(score.classifier?.coverage).toMatchObject({ hasLinkContext: true, hasParentPage: true, hasDestinationPage: true });       expect(score.classifier?.link.matchedTerms).toEqual(expect.arrayContaining(["apt29", "cve-2026-9999", "exploit"]));       expect(score.classifier?.parent.score).toBeGreaterThan(0.45);       expect(score.classifier?.destination.score).toBeGreaterThan(0.5);     }     const byStrategy = Object.fromEntries(scores.map((score) => [score.strategy, score]));     expect(byStrategy.destination_only.classifier?.weights.destination).toBeGreaterThan(0.65);     expect(byStrategy.link_only.classifier?.weights.link).toBeGreaterThan(0.65);     expect(byStrategy.parent_only.classifier?.weights.parent).toBeGreaterThan(0.65);     expect(byStrategy.precision.classifier?.tradeoff).toBe("precision");     expect(byStrategy.recall.classifier?.tradeoff).toBe("recall");     expect(byStrategy.efficiency.classifier?.tradeoff).toBe("efficiency");     expect(byStrategy.hybrid_dynamic.classifier?.selectedStrategy).toBe("hybrid_dynamic_classifier");   });    test("handles missing destination evidence and link/destination disagreement", () => {     const ambiguous = { source, url: "https://example.test/security/weekly-roundup", discoveredAt: new Date().toISOString(), anchorText: "APT29 mention in weekly roundup", surroundingText: "vendor telemetry briefly mentions threat actor activity", parentTitle: "Threat research index", parentText: "Parent index links to ransomware, malware, CVE, and actor reports.", parentRelevance: 0.78, novelty: 0.8, freshness: 0.8 };     const destinationOnly = new FocusedFrontier({ strategy: "destination_only" }).score(ambiguous);     const hybrid = new FocusedFrontier({ strategy: "hybrid_dynamic" }).score(ambiguous);     expect(destinationOnly.decision).toBe("drop");     expect(destinationOnly.classifier?.coverage.hasDestinationPage).toBe(false);     expect(hybrid.total).toBeGreaterThan(destinationOnly.total);      const misleading = { source, url: "https://example.test/cve-apt29-malware", discoveredAt: new Date().toISOString(), anchorText: "APT29 CVE malware exploit indicators", surroundingText: "threat actor campaign", parentRelevance: 0.7, destinationTitle: "Product launch webinar", destinationText: "Marketing webinar registration and sponsored product update with no threat intelligence details.", destinationRelevance: 0.05, novelty: 0.7, freshness: 0.7 };     const corroborated = { ...misleading, destinationTitle: "APT29 exploitation analysis", destinationText: "Technical report confirms CVE exploitation, malware infrastructure, IOCs, and victim targeting.", destinationRelevance: 0.88 };     expect(new FocusedFrontier({ strategy: "precision" }).score(corroborated).total).toBeGreaterThan(new FocusedFrontier({ strategy: "precision" }).score(misleading).total + 0.12);     expect(new FocusedFrontier({ strategy: "precision" }).score(misleading).decision).not.toBe("enqueue");     expect(new FocusedFrontier({ strategy: "precision" }).score(corroborated).decision).toBe("enqueue");   });    test("downgrades high-risk clear-web candidates for review", () => {     const score = new FocusedFrontier({ strategy: "balanced" }).add({ source: { ...source, risk: "high", approvedAt: new Date(0).toISOString(), approvedBy: "analyst_1", governance: { approvalRequired: true, approvalState: "approved", metadataOnly: false, approvedAt: new Date(0).toISOString(), approvedBy: "analyst_1", riskJustification: "High-risk clear-web fixture approved for review-only scheduling." } }, url: "https://example.test/leak", discoveredAt: new Date().toISOString(), anchorText: "ransomware leak victim CVE exploit", surroundingText: "APT malware campaign indicators", parentRelevance: 0.85, destinationRelevance: 0.6, novelty: 0.9, freshness: 0.9, safetyRisk: 0.7 });     expect(score.decision).toBe("review");   }); });
