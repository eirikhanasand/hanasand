import type { CollectedItem } from "../types.ts";
import { hashContent } from "../utils.ts";
import type { LiveCaptureAdapterKind, LiveCaptureCanaryFixtureClass, LiveCaptureCanaryRowDto, LiveCaptureFailureClass, LiveCaptureFixtureClass, LiveCaptureRuntimeCaptureInput, LiveCaptureRuntimeRowDto, LiveCaptureStatus } from "./liveCaptureTypes.ts";
import { FAILURES } from "./liveCaptureConstants.ts";

export const versionFor = (a: LiveCaptureAdapterKind) => ({ rss_feed: "rss-adapter-v2", static_html: "static-web-adapter-v2", report_index: "report-index-live-capture-v1", public_advisory: "public-advisory-adapter-v1", pdf_report: "pdf-report-adapter-v1" }[a]);
export const mimeAllowlistFor = (a: LiveCaptureAdapterKind) => ({ rss_feed: ["application/rss+xml", "application/atom+xml", "application/xml", "text/xml"], static_html: ["text/html", "application/xhtml+xml", "text/plain"], report_index: ["text/html", "application/xhtml+xml", "text/plain"], public_advisory: ["application/json", "application/rss+xml", "application/atom+xml", "application/xml", "text/xml"], pdf_report: ["application/pdf"] }[a]);
export const confidence = (a: LiveCaptureAdapterKind, item?: CollectedItem) => !item ? 0 : a === "public_advisory" ? 0.78 : a === "pdf_report" ? 0.74 : a === "rss_feed" ? 0.72 : item.rawText.length > 200 ? 0.74 : 0.48;
export const sourceFamily = (i: LiveCaptureRuntimeCaptureInput) => str(i.result.items[0]?.metadata.connectorFamily) || (i.adapter === "rss_feed" ? "rss_feed" : i.adapter === "static_html" ? (i.source.tags?.some((t) => /cert|government/i.test(t)) ? "cert_html" : "vendor_blog_html") : i.adapter === "report_index" ? "public_report_index" : str(i.source.metadata?.sourceFamily) || "public_advisory");
export const fixtureClassFor = (r: LiveCaptureRuntimeRowDto): LiveCaptureFixtureClass => r.adapter === "rss_feed" ? "rss_atom" : r.adapter === "report_index" ? "report_index" : r.adapter === "static_html" && r.sourceId.toLowerCase().includes("cert") ? "cert_html" : r.adapter === "static_html" ? "vendor_blog_html" : r.agent06Handoff.sourceMetadata.connectorFamily === "github_advisory" ? "github_security_advisory" : r.agent06Handoff.sourceMetadata.connectorFamily === "cert_government" ? "cisa_kev" : "vendor_advisory_json";
export const inferRun = (r: LiveCaptureRuntimeRowDto) => ["http_error", "rate_limited"].includes(r.failureClass) ? "source_outage" : r.failureClass === "duplicate_content" ? "repeat_run" : r.parserConfidence < 0.65 || r.status === "failed" ? "parser_regression" : "first_run";
export const canaryFixtureClassesFor = (r: LiveCaptureCanaryRowDto): LiveCaptureCanaryFixtureClass[] => [r.adapterFamily === "pdf_report" ? "pdf_text_layer_report" : "", r.parserRepair.category === "unsupported_mime" ? "unsupported_mime" : "", r.parserRepair.category === "unsafe_link_suppression" ? "hostile_unsafe_link_suppression" : ""].filter((v): v is LiveCaptureCanaryFixtureClass => Boolean(v));
export const shortageRows = (rows: LiveCaptureCanaryRowDto[], mins: Record<string, number>) => Object.entries(mins).flatMap(([sourceFamily, required]) => { const observedPromotable = rows.filter((r) => r.adapterFamily === sourceFamily && r.canary.state === "promote").length; return observedPromotable < required ? [{ sourceFamily, required, observedPromotable }] : []; });
export const claimIds = (sourceId: string, item?: CollectedItem) => !item ? [] : [...new Set([...item.rawText.matchAll(/\b(CVE-\d{4}-\d{4,}|APT\d{2}|Akira|LockBit|Lazarus|Turla|Sandworm|Volt Typhoon)\b/gi)].map((m) => m[0]))].slice(0, 8).map((e) => `claim_candidate_${hashContent(`${sourceId}:${e}:${item.contentHash}`).slice(0, 16)}`);
export const count = (rows: LiveCaptureRuntimeRowDto[], status: LiveCaptureStatus) => rows.filter((r) => r.status === status).length;
export const failureCounts = (rows: LiveCaptureRuntimeRowDto[]) => Object.fromEntries(FAILURES.map((f) => [f, rows.filter((r) => r.failureClass === f).length]));
export const order = <T extends string>(order: T[], values: T[]) => order.filter((v) => values.includes(v));
export const str = (v: unknown) => typeof v === "string" && v.length ? v : undefined;
export const num = (v: unknown, key?: string) => { const candidate = key && typeof v === "object" && v ? (v as any)[key] : v; return typeof candidate === "number" && Number.isFinite(candidate) ? candidate : undefined; };
export const strings = (v: unknown) => Array.isArray(v) ? v.filter((x): x is string => typeof x === "string") : [];
export const repairRecommendation = (c: string, row: LiveCaptureRuntimeRowDto) => c === "none" ? "parser healthy; no repair action" : c === "unsupported_mime" ? `add MIME guard or parser support for ${row.runtimeCaps.contentType ?? "unknown content type"}` : c.replaceAll("_", " ");
