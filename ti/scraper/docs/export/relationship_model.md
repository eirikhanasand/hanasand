# Relationship And Export Model

Agent 08 owns the scraper exchange layer. The first implementation keeps this layer backend-neutral: it accepts `PipelineResult` values and emits graph contracts or STIX-like 2.1 bundles without depending on storage.

## Graph Contracts

- Nodes represent actors, aliases, campaigns, victims, sectors, countries/regions, malware/tools, ATT&CK attack patterns, indicators/infrastructure, vulnerabilities, incidents, sources, reports, captures, and sightings.
- Relationships are evidence-backed edges: `alias-of`, `attributed-to`, `targets`, `uses`, `indicates`, `exploits`, `communicates-with`, `derived-from`, `mentions`, `located-in`, `active-in`, `observed-in`, `sighted`, and `related-to`.
- Every relationship must include confidence, first/last seen timestamps, and extraction provenance.
- Edges without provenance are dropped by the relationship builder.
- Multi-entity edges such as actor-targets-victim, actor-uses-malware, actor-uses-TTP, and actor-exploits-CVE require provenance on both sides.
- Actor-result DTOs are produced by `buildActorResultDto`; Agent 09 can expose that DTO directly so API consumers do not need to understand graph internals.

## Relationship Confidence Rules

| Rule | Relationship | Weight | Notes |
| --- | --- | ---: | --- |
| `incident-mentions-entity` | incident `mentions` entity | 0.72 | Contextual mention only; useful for traceability but not strong attribution. |
| `indicator-indicates-incident` | indicator `indicates` incident | 0.90 | Combines indicator and incident confidence. |
| `incident-attributed-to-actor` | incident `attributed-to` actor | 0.86 | Conservative until corroborated by another source or analyst review. |
| `actor-targets-victim` | actor `targets` victim | 0.82 | Requires grounded actor and victim evidence. |
| `actor-uses-ttp` | actor `uses` ATT&CK/TTP | 0.78 | Kept lower because TTP extraction can be ambiguous in prose. |
| `actor-uses-malware` | actor `uses` malware | 0.88 | Stronger when explicit malware evidence is present. |
| `actor-exploits-cve` | actor `exploits` CVE | 0.84 | Requires actor and vulnerability evidence; stays below raw CVE confidence. |

Actor-result aggregation adds support-count, provenance-count, recency, stale/historical flags, and contested-attribution flags. Repeated support raises confidence modestly; contested attribution and stale context downgrade how items appear in ranked result sections.

## Actor Result Ranking

The actor result DTO ranks graph-backed relationships for pages such as an `APT29` result:

- `recent-activity`: actor-connected relationships sorted by `lastSeenAt`.
- `supported-target`: actor-target-victim edges sorted by support and confidence.
- `target-sector`: victim-sector edges for sector summaries.
- `target-region`: victim-country/region edges for geographic summaries.
- `confident-ttp`: actor-uses-ATT&CK/TTP edges.
- `malware-tooling`: actor-uses-malware/tool edges.
- `cve`: actor-exploits-CVE edges.
- `emerging-infrastructure`: indicator/infrastructure evidence sorted by recency.
- `stale-context`: relationships older than the configured stale threshold.
- `contested-claim`: relationships affected by conflicting attribution.

## Progressive Graph Updates

Live search can emit graph relationships before every source has been captured and extracted. These stages are deliberately explicit:

- `discovery`: low-confidence snippet or source-discovery evidence.
- `captured`: a capture exists, but extraction/review may still be partial.
- `extracted`: deterministic extractor output exists.
- `reviewed`: analyst or trusted review has accepted the extraction.
- `promoted`: relationship is ready for normal actor-result ranking and default exchange export.

`buildProgressiveGraphUpdate` merges staged evidence into graph relationships and emits polling deltas: `added`, `updated`, `downgraded`, `contradicted`, `stale`, and `promoted`. Confidence increases with stage and repeated provenance support, but contradictions and stale context lower operational usefulness.

For `/v1/intel/search`, callers should consume `relationshipDeltas`, the compact API-facing delta DTOs, instead of diffing full relationship arrays. Each DTO includes `relationshipId`, change `kind`, evidence `stage`, confidence before/after, source IDs, first/last seen timestamps, review requirements, review state, review reason, available review actions, STIX eligibility flags, endpoint refs, relationship type, and deterministic rank.

Delta ranking is intentionally operational rather than chronological: promoted relationships rank first, followed by contradictions, downgrades, additions, routine updates, and stale churn. Contradicted, downgraded, stale, discovery-only, and low-confidence relationships set `requiresAnalystReview` with review reasons for Agent 07/API summary text.

Discovery-only and unreviewed relationships are excluded from STIX export by default. Default progressive STIX export includes only relationships that are accepted by review or promoted by the graph stage. Callers must opt in with `includeUnreviewedDiscoveryContext` or the compatibility alias `includeDiscoveryEvidence` when they intentionally want discovery-stage context in exchange bundles.

Relationship-level STIX eligibility is exposed as `discoveryOnly`, `captureBacked`, `extracted`, `reviewed`, `promoted`, `accepted`, and `includedByDefault`. STIX relationship objects preserve the same flags under `x_ti_stix_eligibility`.

## Analyst Graph Review

Graph relationships carry review state through `reviewState`: `unreviewed`, `needs_review`, `accepted`, `rejected`, `superseded`, `contradicted`, or `expired`. `applyGraphReviewDecision` and `applyRelationshipReviewDecision` update relationships from audit-safe decisions that include reviewer ID, reason, timestamp, source IDs, evidence IDs, and optional supersession links.

Review decisions append immutable-style `reviewAudit` entries to relationship properties. Agent 06 should persist those audit entries append-only when graph review storage moves out of in-memory DTOs. Agent 07 caveats remain source-text warnings; Agent 08 review state is the graph-level enterprise CTI decision.

## Persisted Graph Views

`buildPersistedGraphSnapshot` converts an in-memory `RelationshipGraph` into storage-oriented contracts for Agent 06 and API-oriented query builders for Agent 09. The snapshot separates persisted nodes, persisted relationships, evidence support records, review audit, confidence history, temporal bounds, and export eligibility.

Query views are intentionally denormalized:

- `buildActorProfileGraphView`: actor node, aliases, neighborhood, ATT&CK matrix cells, and provenance panels.
- `buildVictimProfileGraphView`: victim node, targeting relationships, sector/region relationships, and provenance panels.
- `buildIncidentTimelineView`: relationship events sorted by last-seen time.
- `buildGraphNeighborhoodView`: bounded graph neighborhood with relationship review-state filters.
- `buildAttackMatrixView`: actor/TTP relationships grouped by ATT&CK tactic.
- `buildSourceProvenancePanel`: evidence support plus review audit for a relationship.
- `buildStixExportPreview`: relationship-level preview of what default STIX export includes or excludes.
- `buildRelationshipCursorDeltas`: cursor-visible relationship deltas for API polling.

Cursor relationship kinds are normalized for Agent 09: `actor-target`, `actor-ttp`, `actor-tool`, `actor-malware`, `victim-sector`, `victim-country`, `incident-source`, `indicator-infrastructure`, and `evidence-provenance`. The cursor DTO preserves before/after confidence, workflow state, review state, source/target labels, source IDs, evidence IDs, export eligibility, and a stable cursor.

Analyst workflow state is exposed in API language as `proposed`, `accepted`, `rejected`, `downgraded`, `superseded`, `stale`, `contradiction`, or `needs-human-review`. This maps over the persisted review states without forcing API consumers to know every storage detail.

`downgradeAndExpireStaleRelationships` is the contract for scheduled stale-fact maintenance. Stale relationships are downgraded into review; expired relationships receive an expiry audit entry and a low confidence ceiling so old graph facts do not remain authoritative forever.

## Graph Integrity Gates

`buildGraphIntegrityReport` checks persisted graph snapshots before UI panels or STIX exports consume them. It reports unsupported edges, weak discovery-only edges, contradicted edges, stale accepted edges, orphan relationships, missing provenance, missing evidence ledger IDs, restricted-only/source-biased support, unreviewed victim/CVE/TTP claims, and export schema risk.

`buildGraphReviewBatch` turns integrity findings into analyst work items with actions: accept, reject, downgrade, supersede, request evidence, or mark stale. These batches are API-ready for Agent 09 and should preserve Agent 07 quality note context without making extraction caveats the final graph state.

`checkStixExportReadiness` applies export-specific gates: discovery-only inclusion policy, minimum confidence, accepted-relationship requirements, contradictory/stale blockers, and complete evidence provenance. It should run before STIX/TAXII-style exchange export.

`buildGraphCutoverReport` is the compact rehearsal artifact for scraper-native search promotion. It combines graph integrity, STIX export readiness, review-batch state, STIX export preview, API readiness sections, and promotion blockers into one DTO. The report includes sections for actor profile, victim profile, incident timeline, ATT&CK matrix, graph neighborhood, provenance panel, and STIX export preview.

Promotion blockers use stable reason codes for Agent 09 and Agent 10: graph integrity finding codes plus `review_queue_open`, `no_export_ready_relationships`, `provenance_incomplete`, and `contradictory_or_stale_edges`. Review queues are capped by `maxReviewItems` while preserving deterministic ordering, so high-volume actor searches remain API-safe without hiding total queue counts.

`buildGraphReviewApplyPlan` converts graph review batches into dry-run apply plans. Supported actions are `accept_edge`, `reject_edge`, `downgrade_edge`, `supersede_edge`, `request_evidence`, `mark_stale`, `expire_edge`, `hold_edge`, and `block_export`. Each plan item includes preconditions, evidence IDs, confidence impact, export impact, audit notes, rollback notes, source, and safety classification: `automation_safe`, `human_approval_required`, `blocked`, or `rollback_only`.

Discovery-only weak relationships must remain `block_export` plans until stronger evidence and review preconditions pass. The apply-plan layer is intentionally non-mutating; graph changes still require explicit review decisions and audit persistence.

`buildGraphReviewPlanApiDto`, `buildGraphCutoverReportApiDto`, and `buildStixExportReadinessApiDto` are the compact API wrappers for `/v1/graph/review-plan`, `/v1/graph/cutover-report`, and `/v1/exports/stix`. They preserve the underlying plan/report/readiness contracts while making endpoint, status, blocker, preview, SLA, audit, and rollback fields explicit for Agent 09 response shapes and Agent 10 promotion gates.

`graphReviewApiExamples` freezes example payloads for accept, reject, downgrade, supersede, request evidence, mark stale, block export, and discovery-only manual-review-required. The discovery-only example is intentionally blocked and non-exportable so automation cannot turn weak discovery evidence into STIX-ready graph state without capture, extraction, and review support.

`handleGraphReviewPlanRoute`, `handleGraphCutoverReportRoute`, and `handleStixExportReadinessRoute` provide the mountable dry-run route-handler boundary for those DTOs. The handlers validate `relationshipId`, reject unsupported `selectedActions`, expose the frozen examples on request, and never apply review decisions, publish bundles, or mutate graph state.

`buildCorrelationGraphQuery` and `buildCorrelationTimeline` are the production query contracts for `/v1/graph/query` and `/v1/graph/timeline`. They expose actor-victim-malware-tool-CVE-TTP-source relationships with confidence, first/last seen timestamps, provenance IDs, source IDs, capture IDs, content hashes, evidence ledger IDs, evidence IDs, review state, workflow state, full export eligibility, relationship deltas, ATT&CK matrix cells, and provenance panels.

`buildGraphQueryApiContract` freezes the compact API contract sections consumed by the CTI frontend and Agent 09: actor, victim, campaign, TTP, malware/tool, CVE, infrastructure, sector, region, and source neighborhoods plus victim profile, incident timeline, ATT&CK matrix, relationship deltas, export readiness, and STIX preview. The contract also embeds the STIX 2.1 mapping contract so API documentation can show the same object, relationship, marking, external-reference, and `x_ti_*` provenance semantics as the exporter.

`CorrelationGraphQueryDto.neighborhoods` groups query results by production CTI facets. Each facet includes node types, node IDs, relationship IDs, maximum confidence, review states, freshness, export-ready counts, and export-hold counts. This lets actor pages show current-vs-historical TTP drift, CVE exploitation claims, infrastructure, sectors, regions, campaigns, sources, and ransomware victim neighborhoods without reading raw storage rows.

`CorrelationGraphQueryDto.relationships` now carries the enforcement state needed by query consumers directly on each edge: contradiction markers, source-family bias, evidence gap codes, answer caveats, provenance IDs, evidence ledger IDs, review state, confidence, time bounds, and export eligibility. `CorrelationGraphQueryDto.readinessFacets` summarizes actor profile, victim profile, campaign timeline, ATT&CK matrix, infrastructure pivots, source-family bias, evidence gaps, STIX bundle readiness, and TAXII collection metadata so `/v1/graph/query` can answer CTI panel readiness without re-deriving release-gate logic.

`GraphExportCertificationDto` is the release-candidate certification packet for graph query and STIX export. It freezes scenario fixtures for APT29, Scattered Spider, Akira, Turla, CVE exploitation, weak co-mentions, restricted-only evidence, missing ledger IDs, schema-risk exports, missing provenance, contradicted relationships, stale relationships, and analyst-reviewed promotion. The packet appears on graph runtime, `/v1/graph/query`, `/v1/graph/review-plan`, `/v1/exports/stix`, `/v1/intel/search.graph`, and the `/v1/contracts` index, and it includes proof routes, `noUnsupportedTaxiiServerClaims`, and `rcGate` with Agent 10 `graphStixReleaseCandidateGate` proof metadata plus descriptor-only TAXII boundaries.

`GraphLiveSearchUpdateDto` is the responsive live-search graph packet on `CorrelationGraphQueryDto.liveUpdate`, `GraphRuntimeApiDto.liveUpdate`, `/v1/intel/search.graph`, `/v1/graph/query`, and `/v1/exports/stix` runtime payloads. It freezes Task AA incremental scenarios for APT29, APT42, Turla, Volt Typhoon, Scattered Spider, Akira, CVE exploitation, random actors, weak co-mentions, public-channel-only hints, restricted-held evidence, missing ledger IDs, stale/contradicted relationships, missing provenance, accepted promotion, and STIX export eligibility. It keeps `nextPollSeconds: 3`, `cursorField: "graph.deltas[].cursor"`, weak discovery as pivots/caveats only, public-channel-only hints held until corroborated or reviewed, restricted evidence as held context, STIX export limited to reviewed/promoted relationships, and TAXII descriptor-only. Its `deltaStream` fixture contract adds Task AB review/export holds for clear-web capture promotion, public-channel hints, restricted metadata holds, claim-ledger holds, missing ledger IDs, weak co-mention pivots, actor alias collisions, contradicted attribution, stale TTPs, new victim/CVE claims, malware/tool and infrastructure relations, analyst accepted/rejected decisions, graph rollback, and STIX eligibility changes.

`buildGraphReviewQueueSummary` is the shared review queue rollup for graph query, review-plan, STIX readiness, and `/v1/intel/search.graph` responses. It reports total queued relationships, export holds, human-review work, automation candidates, blocker-code counts, workflow-state counts, and top relationship IDs. Public `/ti` views should use `publicFactPolicy: "hold_weak_edges"` as the signal to keep graph claims out of fact panels until review/provenance gates pass.

These contracts can move from in-memory rows to Postgres graph tables, Neo4j, or another graph store without changing API consumers as long as the exported DTOs remain stable. Storage should preserve node IDs, relationship IDs, evidence support IDs, audit entries, confidence history, and cursor ordering semantics.

## ATT&CK Mapping

ATT&CK candidates are produced only from extracted TTP entities with provenance. Explicit IDs such as `T1566` are preserved but still flagged for analyst confirmation when the technique name is not known. Heuristic phrase mappings include review reasons when confidence is low.

Agent 07 extraction fixtures now include expected TTP hints and uncertainty labels. Agent 08 mapping should continue to consume only grounded `ttp` entities with provenance from the pipeline, not raw prose matches or unprovenanced fixture expectations.

## STIX 2.1 Export

The STIX export returns a `bundle` with identity, report, indicator, intrusion-set, malware/tool, vulnerability, attack-pattern, observed-data, and relationship objects as needed. Scraper-specific confidence/provenance fields use `x_ti_*` custom properties so downstream OpenCTI/MISP-style integrations can preserve why an object exists.

STIX-like bundles must pass `validateStixBundle` before being exposed to API/export consumers. Validation checks bundle/object IDs, STIX 2.1 version markers, relationship refs, indicator patterns, confidence bounds, report refs, and relationship provenance.

`STIX_21_GRAPH_MAPPING_CONTRACT` freezes mapping semantics for threat actors (`intrusion-set`), malware/ransomware/tools (`malware` with labels), ATT&CK techniques (`attack-pattern` plus MITRE external references), indicators, vulnerabilities, reports, STIX relationships, review-required markings, evidence external references, and custom provenance fields. `actor-vulnerability`, victim-sector, victim-country, actor-malware, actor-target, and actor-TTP edges are first-class graph query relationships.

Task AD hardening keeps confidence mapping explicit as graph `0..1` confidence to STIX `0..100` integer confidence, requires MITRE ATT&CK external IDs to use `T####` or `T####.###`, and treats revoked/deprecated ATT&CK techniques as review metadata until a replacement technique is selected. The mapping contract includes fixtures for actor-tool, actor-malware, actor-CVE, actor-victim, campaign-TTP, and deprecated ATT&CK cases, while preserving TAXII as descriptor-only.

`exportGraphSnapshotToStixBundle` is the cutover-safe graph exporter. It emits only readiness-passing evidence-backed graph relationships as STIX relationship facts, includes markings and external references, and records blocked weak, rejected, unsupported, contradicted, stale, discovery-only, or missing-provenance relationships under `x_ti_blocked_relationships` instead of flattening them into facts.

Graph drift detection now covers stale accepted edges, contradicted edges/source-family conflicts, missing provenance, weak discovery-only edges, explicit source-bias clusters, restricted-only claims, unsupported restricted-metadata relationships, unreviewed victim claims, unreviewed CVE exploitation claims, and unreviewed actor-to-TTP mappings. Source-bias, CVE, TTP, victim, and restricted-metadata findings are export holds by design: they should request corroborating evidence or a reviewed safe mapping before public/API fact promotion. `StixExportReadinessApiDto.reviewActions` exposes the dry-run analyst action slice for these holds so UI/API callers can show accept, reject, downgrade, expire, and hold actions without mutating the graph.

`GraphRuntimeApiDto` is the compact runtime contract shared by `/v1/intel/search.graph`, `/v1/graph/query`, and `/v1/exports/stix`. It carries relationship confidence, evidence ledger IDs, review state, freshness, export-ready status, export hold codes, `liveUpdate`, and the same review queue public fact policy used by answer fusion and STIX export readiness.

`CorrelationGraphQueryDto.investigationWorkspace` is the Task AE analyst workspace contract for `/v1/graph/query`. It groups actor, victim/company, malware/tool, CVE, campaign, infrastructure, TTP, source, capture, claim, and incident nodes by investigation type; exposes a relationship confidence ledger with supporting evidence IDs, ledger IDs, capture IDs, source IDs, contradiction/stale/review-hold state, export blockers, and allowed review actions; and preserves `graph.deltas[].cursor` with `nextPollSeconds: 3` for Agent 09 polling. The workspace is read-only and safe for analyst UI use: restricted or leak-adjacent material is represented as metadata-only review-held pivots, raw restricted URLs/content are not serialized, TAXII remains descriptor-only, and promotion/export requires review plus evidence-ledger completeness.

`GraphBackendRepositoryContractDto` is the Task AC persistence bridge for moving from in-memory snapshots to SQL graph tables, Neo4j, or another graph backend later. It freezes tenant-scoped operations for node and relationship upserts, provenance append rows, append-only review decisions, confidence history, cursor deltas, and export eligibility flags. It also names Agent 06 ledger persistence, Agent 07 stable entity/review-state preservation, Agent 09 DTO stability, and Agent 10 replay verification before graph/STIX promotion.

`GraphBackendCutoverRehearsalDto` is the Task AF production graph backend rehearsal packet. It keeps the repository target storage-agnostic while making the cutover inspectable: Postgres graph table and Neo4j-compatible schema sketches, tenant-scoped indexes, Agent 06 evidence/claim-ledger replay order, stale/contradicted/review-held relationship imports, cursor continuity, append-only review/confidence history, export eligibility recomputation, backup/restore manifests, and rollback paths. The packet appears on `GraphRuntimeApiDto.backendCutover` so `/v1/graph/query`, `/v1/intel/search.graph`, and `/v1/exports/stix` can expose the same Agent 10 release field without changing graph query consumers. Weak discovery, public-channel hints, and restricted metadata remain pivots or review holds until capture, extraction, review, and evidence-ledger completeness promote them.

Task AK adds `GraphBackendPerformanceSoakDto` on `GraphRuntimeApiDto.backendPerformance`. It is a backend-neutral query-cost and soak contract for embedded memory, Postgres graph tables, and future Neo4j candidates. The packet reports bounded single-hop query cost, node/edge/evidence/cursor/review-hold counts, p95/p99 latency targets, queue pressure, 3-second public polling budgets, and soak scenarios for actor query, campaign timeline, STIX export preview, cursor replay, and review-hold bursts. Agent 10 can read `graphBackendPerformanceSoak` to promote, warn, hold, or rollback graph backend rollout without changing `/v1/graph/query` response shape. Restricted material remains metadata-only, tenant scope is required, and TAXII remains descriptor-only.

`GraphAttackCampaignWorkspaceDto` is the Task AG query-workspace contract for ATT&CK technique timelines and campaign graphs. It appears on `CorrelationGraphQueryDto.attackCampaignWorkspace` and keeps actor-to-TTP edges analyst-usable: each technique event includes MITRE technique ID when available, tactic, first/last seen, confidence trend, stale/contradicted/review-held state, evidence IDs, Agent 06 ledger IDs, source IDs, export eligibility, and export blockers. The campaign graph connects actors, campaigns, TTPs, malware/tools, victims, infrastructure, and vulnerabilities through relationship IDs rather than raw text.

Task AG does not change export promotion semantics. Stale TTPs, contradicted campaign claims, weak discovery-only edges, public-channel-only hints, restricted metadata, and missing-ledger relationships remain review holds or pivots until capture, extraction, analyst review, and evidence-ledger completeness make them eligible. TAXII remains descriptor-only; the workspace exposes the same 3-second graph delta cursor contract used by `/v1/graph/query`.

Task AH adds bounded pivot and performance-budget metadata to the same workspace. `pivotQueues` ranks actor, campaign, TTP, malware/tool, victim, infrastructure, and vulnerability pivots with next actions such as opening a bounded neighborhood, requesting evidence, holding export, or promoting reviewed edges. `performanceBudget` caps nodes, edges, technique timeline events, and review holds so `/v1/graph/query` can stay compatible with public `/ti` polling and Agent 09’s 3-second cursor contract. Truncated workspaces expose `nextPageCursor` when a matching graph delta cursor is available.

Task AI adds `searchPivotRecommendations` for graph-to-search handoff. Recommendations turn bounded graph pivots into safe follow-up query strings such as `actor:APT29`, `campaign:...`, `technique:T1566 Phishing`, `tooling:...`, `victim:...`, `infrastructure:...`, or `cve:...`. They state expected effect (`open_graph_neighborhood`, `request_more_evidence`, `corroborate_hold`, or `promote_reviewed_fact`), source relationship IDs, confidence, and priority while guaranteeing `willStartCrawling: false`, `willFetchRestrictedMaterial: false`, metadata-only output, and descriptor-only TAXII behavior. Held graph facts remain held until the normal evidence/review/export gates pass.

Task AJ adds `investigationWorkspace.workflowContracts` for analyst graph workflows without standing up mutable graph services yet. `/v1/graph/query` now carries API-ready contracts for opening an investigation, saving a bounded pivot set, reviewing relationships, and exporting a reviewed STIX subset. These contracts are tenant-scoped, cursor-stable, provenance-required, and dry-run/preview-only until a persistent review store backs the action. Review payloads must include relationship, evidence, ledger, source, reviewer, and reason fields; export previews include only eligibility-passing relationship IDs while held, stale, contradicted, weak discovery, restricted, and missing-ledger edges remain blocked. TAXII remains descriptor-only.

Task AL adds `attackCampaignWorkspace.reviewBoard` as the enterprise campaign timeline review board. The board groups campaign/TTP relationship rows into `ready_for_public_fact`, `needs_evidence`, `stale_or_contradicted`, `restricted_or_policy_hold`, and `export_blocked` lanes, with relationship IDs, actor IDs, campaign IDs, technique IDs and ATT&CK IDs, first/last seen timestamps, confidence trend, review/workflow state, evidence IDs, ledger IDs, source IDs, export eligibility, recommended action, and release impact. The board is metadata-only: restricted or leak-adjacent material remains held as descriptors, stale and contradicted edges stay out of public facts and STIX promotion, and TAXII remains descriptor-only until reviewed export eligibility is explicit.

Task AM adds graph review persistence and reviewed export subset governance. `GraphReviewPersistenceLedgerDto` freezes append-only review decisions for promote, hold, reject, stale, contradicted, duplicate-merge, split-alias, and export-ready actions with reviewer, source, claim, evidence, ledger, capture, quality-correction, rollback, and cursor-continuity fields. Rollback is represented as a compensating review decision; implementations must not delete audit rows or rewrite evidence history. `GraphReviewedExportSubsetGovernanceDto` explains which reviewed relationship IDs may enter STIX 2.1 export, why other IDs remain held, how ATT&CK freshness and campaign timeline review are enforced, and why TAXII remains descriptor-only until a coordinated server boundary exists. Both DTOs are exposed on the graph investigation workspace, graph runtime, graph review plan, STIX readiness, and STIX bundle identity metadata while preserving metadata-only restricted handling and no-leak serialization.

Task AN adds `GraphQueryCostControlsDto`, the tenant/workspace budget packet for analyst investigations, campaign timelines, STIX previews, and runtime graph deltas. The packet counts nodes, edges, relationship review rows, technique timeline events, campaign pivots, evidence joins, STIX preview rows, cursor continuation rows, and export-eligibility recomputation against soft/hard limits. Budget pressure degrades predictably through bounded relationship IDs, `graph.deltas[].cursor` continuation, graph-expansion holds, export holds, or rollback-to-summary. Weak discovery, public-channel-only, restricted metadata, stale, contradicted, and missing-ledger relationships stay in held-fact lists and must not be promoted just because a query was truncated. The same cost-control packet is exposed on the investigation workspace, campaign workspace, and graph runtime with Agent 02 scheduler budget, Agent 06 evidence-chain, Agent 07 quality/confidence, Agent 09 stable API, and Agent 10 capacity/runbook handoffs.

Task AO adds `GraphRelationshipDriftMonitorDto`, the campaign relationship drift monitor shared by graph investigation workspaces, ATT&CK campaign workspaces, runtime graph APIs, and STIX readiness. It classifies actor aliases, campaign membership, TTP mappings, malware/tool links, CVE links, sector/country targeting, victim claims, infrastructure hints, and leak/dataset metadata holds into actions such as keep promoted, demote to review, mark stale, mark contradicted, split alias, merge duplicate, block STIX export, request evidence replay, request source expansion, or preserve historical. Drift signals cover fresh public advisories, source-pack expansion, public-channel hints, restricted metadata review, evidence replay failures, analyst corrections, source retirement, campaign/TTP/victim/infrastructure changes, and dataset metadata holds. The monitor respects `GraphQueryCostControlsDto`: weak discovery, public-channel-only, restricted metadata, stale, contradicted, missing-ledger, and budget-bounded rows remain non-exportable after drift evaluation. Agent 06 receives evidence replay requests, Agent 07 receives contradiction/alias split rows, Agent 09 receives 3-second polling drift events, Agent 10 receives release holds, and Agent 01/04 receive source expansion requests without exposing raw restricted material or TAXII server behavior.

Task AP adds analyst-grade graph explainability and notebook export contracts. `GraphRelationshipExplainabilityDto` explains each promoted, held, stale, contradicted, split, merged, export-ready, or export-blocked relationship with source IDs, evidence IDs, claim ledger refs, review decision IDs, confidence trend, drift action/signals, ATT&CK/campaign context, export eligibility, caveats, and Agent 06/07/09/10 handoffs. `GraphInvestigationNotebookExportDto` packages a bounded, metadata-only investigation notebook with nodes, edges, pivot recommendations, caveats, relationship explanation rows, STIX preview descriptors, TAXII descriptor-only boundaries, and query cost/budget metadata. Notebook exports are preview-only and do not start crawling, fetch restricted material, include object keys, serialize raw restricted/leak content, or weaken holds for weak discovery, public-channel-only, restricted metadata, stale, contradicted, missing-ledger, or budget-bounded relationships.

Task AQ adds `GraphBackendMigrationCertificationDto`, the production migration certification packet for moving graph workspace, review, confidence, notebook, and STIX preview behavior from in-memory contracts to graph tables or a Neo4j-compatible backend later. The packet is exposed on `GraphRuntimeApiDto.backendMigrationCertification`, `GraphInvestigationWorkspaceDto.backendMigrationCertification`, and `StixExportReadinessApiDto.backendMigrationCertification`. It lists migration order for nodes, relationships, evidence provenance, relationship reviews, confidence history, cursor deltas, ATT&CK/campaign timelines, graph pivots, notebook export projections, and STIX preview subsets; names replay prerequisites from Agent 06; requires tenant/workspace scoping, indexes, cursor continuity, rollback checkpoints, and export eligibility recomputation; and gives Agent 07/09/10 the fields needed to certify quality gates, API shape stability, and promote/hold/rollback decisions. Weak discovery, public-channel-only, restricted metadata, stale, contradicted, missing-ledger, and budget-bounded relationships remain non-exportable until review, replay, and recomputation pass.

Program BA.5 adds `GraphAttackCampaignFreshnessSloDto` on `GraphAttackCampaignWorkspaceDto.freshnessSlo`. The packet turns ATT&CK technique/campaign review rows into freshness SLO rows with tactic-specific targets, age, warning/breach/held state, export eligibility after freshness enforcement, source cadence requests, evidence replay requests, and Agent 01/02/04/06/07/09/10 handoffs. It keeps stale, contradicted, unreviewed, missing-ledger, source-gap, restricted, and policy-held campaign/TTP rows out of public facts and STIX export, preserves 3-second `graph.deltas[].cursor` polling, and remains metadata-only with descriptor-only TAXII boundaries.

Program BA.6 adds `GraphNeo4jMigrationAdapterBenchmarkDto` on `GraphRuntimeApiDto.neo4jMigrationAdapter`. This is a contract-only Neo4j adapter benchmark, not a live driver or server. It projects graph nodes into Neo4j-compatible labels, maps relationship types with export-ready and held counts, lists required Cypher constraints/indexes, and defines benchmark shapes for actor one-hop, campaign two-hop, ATT&CK matrix, STIX preview subset, and cursor replay queries. Parity checks require node/relationship/evidence counts, cursor ordering, review state, export eligibility, and held-row non-exportability to match the current runtime DTOs. Postgres graph tables remain the primary cutover backend; Neo4j stays a future compatible target until Agent 06 replay, Agent 07 quality parity, Agent 09 API stability, and Agent 10 release gates pass.

Program BA.7 adds `GraphDeltaClientContractDto` to `GraphLiveSearchUpdateDto.clientContract` and `GraphInvestigationNotebookExportDto.deltaClientContract`. The contract freezes graph delta polling as the primary client path with 3-second intervals, stable `graph.deltas[].cursor` cursors, route bindings, request/response field names, client states for empty polls, new relationships, review holds, export-ready rows, STIX holds, and rollback-required rows, and a notebook binding for metadata-only preview exports. Notebook exports remain bounded to 75 nodes and 50 edges, preview-only, and descriptor-only for TAXII. Clients must render relationship caveats and export holds before promoting graph facts; unsafe payloads, object keys, raw restricted/leak material, and unsafe URLs are never client fields.

Program BB adds `GraphBackendAdapterCutoverContractDto` on `GraphRuntimeApiDto.backendAdapterCutover`. This is the backend-neutral adapter packet for promoting from memory snapshots toward Postgres graph tables while keeping Neo4j as a compatible contract-only target. It maps every repository operation to Postgres tables and Neo4j labels/relationships, carries migration schemas and dry-run migration proof, preserves `graph.deltas[].cursor` replay order, repeats review-hold parity for missing-ledger, stale, contradicted, weak discovery, public-channel-only, restricted metadata, and budget-bounded relationships, and composes performance SLOs plus Neo4j benchmark scenarios into one cutover decision. Postgres remains the primary backend, Neo4j has no live driver, and existing graph/runtime/STIX DTO route shapes must not change. If the adapter cannot replay evidence, cursor, review state, and export eligibility exactly, memory or Postgres graph tables remain authoritative and graph/STIX export stays held.

Program BC extends `GraphAttackCampaignFreshnessSloDto` from age-only freshness into a campaign/TTP release gate. Each row now reports ATT&CK lifecycle state for deprecated or revoked technique IDs, alias-drift state when actor naming conflicts require split/merge review, contradiction state, and an explicit export eligibility decision. Summary counts expose deprecated technique rows, alias-drift rows, contradiction rows, and export-eligible rows. Deprecated ATT&CK mappings, alias drift, contradicted edges, stale/breached rows, missing evidence, and unreviewed/blocked relationships remain held from public facts and STIX export until reviewed replacements, evidence replay, or analyst decisions clear the blockers.

Program BD adds `GraphTaxiiDescriptorStixBundleGovernanceDto` on graph runtime, graph investigation workspace, and STIX readiness responses. It turns reviewed export subset governance into a descriptor-only TAXII/STIX bundle planning packet: eligible, held, excluded, and decision relationship IDs; STIX 2.1 media type; stable page sizing and cursor fields; collection descriptors for `ti-graph-reviewed-stix-21`; bundle gates for reviewed relationships, provenance completeness, ATT&CK lifecycle review, restricted metadata descriptor-only handling, and no discovery-by-default export; and a future `TaxiiExportProvider` request/page contract. It intentionally exposes no mounted TAXII routes, no server implementation claim, no object keys, and no raw restricted/leak material. The packet lets Agent 09/10 reason about export readiness while Agent 08 keeps TAXII as a coordinated future interface.

Program BE adds a first-class `releaseCandidate` graph/STIX gate on graph runtime, graph investigation workspace, and STIX readiness responses. The field surfaces the existing `GraphReleaseCandidateGateDto` next to relationship ledgers, export governance, and TAXII/STIX governance so operators can inspect promote/hold/rollback state, required scenario coverage, public API impact, STIX impact, Agent 10 proof commands, and the descriptor-only TAXII boundary directly from query workspaces and export readiness. It does not add routes or server behavior; it only makes the release-candidate decision explicit and no-leak safe.

Program BF adds `GraphIncidentClaimWorkspaceDto` on `CorrelationGraphQueryDto.incidentClaimWorkspace` and `GraphInvestigationWorkspaceDto.incidentClaims`. The workspace groups incident/claim nodes across public reports without treating every article as a separate attack, then exposes actor, victim/company, campaign, sector, country, malware/tool, TTP, source, and report relationship IDs with first/last reported timestamps, publisher/source-family counts, corroborating and contradicting evidence IDs, ledger IDs, confidence, freshness, review state, and export state. Merge and split semantics are conservative: same incident nodes and corroborating publishers can merge public reports, while alias collisions, contradictory attribution, same-day different victims, old campaign reuse, and recurring victim claims remain review-held or split-required. Reviewed STIX subsets list eligible and held relationship IDs explicitly; unreviewed incident-centered inference, contradicted claims, missing provenance, public-channel-only hints, weak discovery, restricted metadata, stale rows, and missing-ledger rows stay out of authoritative STIX export. The contract remains metadata-only, preserves the descriptor-only TAXII boundary, and gives Agent 04/06/07/09/10 stable handoff fields for conflict resolution, evidence replay, quality gates, frontend rendering, and release decisions.

Program BG adds `GraphActorTimelineChangeWorkspaceDto` on graph query, graph investigation workspace, graph runtime, and STIX readiness responses. The workspace orders actor relationship, incident-claim, ATT&CK, malware/tool, victim, infrastructure, vulnerability, and source events by first/last seen time, then derives campaign-change rows for actor aliases, campaign membership, TTP drift, targeting changes, tooling changes, CVE use, infrastructure changes, source-family changes, and incident-claim changes. Each row preserves relationship IDs, node IDs, evidence IDs, ledger IDs, capture IDs, source IDs/families, confidence trend, freshness, contradiction state, review/workflow state, public fact state, export eligibility, and held blockers. Stale, contradicted, weak discovery, public-channel-only, restricted metadata, missing-ledger, and budget-bounded timeline rows remain non-authoritative and STIX-held until evidence replay and review clear them. TAXII remains descriptor-only and the contract exposes metadata-only data suitable for Agent 09 timeline rendering and Agent 10 release gates without raw restricted/leak material.

Program BH adds `GraphActorProductPacketDto` on graph query, graph investigation workspace, graph runtime, and STIX readiness responses. The packet packages graph-backed actor intelligence for `/ti`, Apify Actor rows, and reviewed STIX previews by combining actor timeline changes, incident claim clusters, victim/sector/country targeting context, TTP/tool/source corroboration, contradiction state, reviewed export readiness, STIX object-type readiness, and compact public copy hints. Unknown or arbitrary actor queries remain honest: when no graph-backed evidence exists, the packet reports searching-safe messaging, missing source families, and safe next pivots rather than borrowing unrelated actor facts. Reviewed relationships may appear as `intrusion-set`, `campaign`, `malware`, `tool`, `attack-pattern`, `identity`, `relationship`, or `sighting` preview candidates; held, stale, contradicted, weak, restricted, public-channel-only, missing-ledger, or unreviewed rows remain held. The output is metadata-only and excludes raw URLs, leaked content, credential/payload evidence, private-channel material, actor interaction, and unsafe dark-web details.

Program BI adds `GraphStixTaxiiMarketplaceReadinessDto` on graph query, graph investigation workspace, graph runtime, and STIX readiness responses. The packet gives marketplace-facing reviewed STIX bundle examples for Apify sample rows, `/ti` previews, and enterprise STIX previews while keeping examples limited to rows that already pass reviewed relationship, provenance, freshness, and no-leak gates. It also exposes descriptor-only TAXII collection and pricing readiness for `ti-graph-reviewed-stix-21` with free sample, analyst, and enterprise tiers, stable media type and cursor/page fields, and an explicit `serverImplemented: false` boundary. Held, stale, contradicted, weak, restricted, public-channel-only, missing-ledger, and unreviewed rows are listed as exclusions rather than authoritative bundle facts. The contract does not add TAXII routes or server behavior and excludes raw URLs, leaked content, credentials/payloads, private-channel material, actor interaction, unsafe dark-web details, object keys, and TAXII server claims.

Program BJ adds `GraphStixTaxiiMonetizationExportContractsDto` on graph query, graph investigation workspace, graph runtime, and STIX readiness responses. The packet turns reviewed graph/STIX readiness into product-grade export contracts for free sample, analyst, and enterprise tiers with row limits, confidence thresholds, freshness windows, update cadence, evidence requirements, pricing-readiness notes, and explicit implemented-surface boundaries. It includes an object eligibility matrix for `intrusion-set`, `campaign`, `malware`, `tool`, `attack-pattern`, `identity`, `relationship`, `sighting`, `indicator`, and `report`, mapping eligible and held relationship IDs to allowed tiers and blockers. Held reasons are normalized into buyer-safe export blockers for weak evidence, stale activity, contradiction, restricted metadata-only, public-channel-only, missing ledger, missing analyst review, unsafe source, tenant policy hold, and TAXII server not implemented. Apify-facing compact fields expose `stixReady`, `taxiiDescriptorReady`, `exportTier`, `exportBlockers`, and `reviewedObjectTypes` without raw URLs, leaked content, credentials, payloads, private-channel material, object keys, actor interaction, or unsafe dark-web details. Enterprise TAXII remains descriptor-only and must not be described as a mounted server.

Program BK adds `GraphActorComparisonNotebookDto` on graph query, graph investigation workspace, graph runtime, and STIX readiness responses. The packet compares graph-backed actor rows by reviewed and held relationships, average confidence, latest-seen timestamps, reviewed STIX object types, source families, export-tier readiness, export blockers, differentiators, and shared pivots for TTPs, malware/tools, victims, and campaigns. It also emits buyer-ready notebook contracts for Apify listing samples, public `/ti` investigations, and enterprise export review with bounded row counts, reviewed/held relationship IDs, section keys, export tier, and next actions. These notebooks are preview/read-only contracts: they do not save mutable workspace state, do not fetch new evidence, do not stand up TAXII, and do not expose raw URLs, leaked content, credentials, payloads, private-channel material, object keys, actor interaction, or unsafe dark-web details.

`GraphExportSlaDto` is the release-gate SLA view for `/v1/intel/search.graph`, `/v1/graph/query`, `/v1/graph/review-plan`, `/v1/exports/stix`, and Agent 10 release packets. It buckets export-ready, held, review-required, stale, contradicted, missing-provenance, restricted-only, weak co-mention, source-biased, unreviewed victim, unreviewed CVE, and unreviewed TTP relationships, and states public-answer/STIX impact plus the Agent 08 proof command.

`GraphExportEnforcementDto` is the compact promotion enforcement view for `/v1/intel/search.graph`, `/v1/graph/query`, `/v1/graph/review-plan`, `/v1/exports/stix`, and Agent 10 release packets. It turns integrity findings into pass, warning, hold, or rollback state, maps each finding to a dry-run analyst action, and exposes public-answer caveats plus release-gate decisions for public answers, STIX promotion, schema safety, and evidence-ledger completeness.

Promotion enforcement holds or rolls back missing ledger IDs, weak co-mentions, restricted-only evidence, unreviewed victim/CVE/TTP claims, stale or contradicted relationships, source-bias clusters, missing provenance, and export schema risk. Agent 07 answer fusion should read `answerCaveats` before promoting graph claims into public text, while Agent 10 release packets should preserve the enforcement proof alongside `graphExportSla`.

## TAXII Boundary

The project exposes only TAXII future-facing interfaces for now: collection descriptors, export page contracts, and a provider interface. A full TAXII server should not be built until API ownership and deployment boundaries are coordinated.

`buildTaxiiCollectionReadiness` emits a TAXII-facing collection descriptor for reviewed STIX 2.1 graph objects with read/write flags, media types, ready/blocked counts, and an opaque future cursor. This is metadata for Agent 09/10 planning only; it must not be treated as a deployed TAXII endpoint.
