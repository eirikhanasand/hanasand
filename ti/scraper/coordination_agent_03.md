Status: done_no_worker

High-value task completed:
- Parser-admission variants now convert first-page Actor output into 40 chargeable findings instead of low-value source-only rows.

Discarded:
- Parser proof, evidence, and report-only tasks.

No further worker task. Continue only with extraction changes that improve actor, victim, target, TTP, dataset, date, confidence, or source pivots in buyer-visible rows.
