{
  "actorOutputSchemaVersion": 1,
  "title": "Threat intelligence output",
  "description": "Normalized public threat actor and ransomware activity rows with buyer summaries, recommended next actions, key pivots, confidence, freshness, and safe metadata.",
  "properties": {
    "dataset": {
      "type": "string",
      "title": "Threat intelligence rows",
      "description": "The default dataset containing normalized public threat intelligence rows.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "summary": {
      "type": "string",
      "title": "Run summary",
      "description": "Run-level counts and buyer sample rows stored as the OUTPUT record.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
    }
  }
}
