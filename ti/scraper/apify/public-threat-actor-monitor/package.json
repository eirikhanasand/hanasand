{
  "name": "public-threat-actor-monitor",
  "version": "0.6.4",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "check": "bunx tsc --noEmit",
    "smoke": "bun run scripts/smoke.ts",
    "check:publication": "bun run scripts/check-publication.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
