import type { MarketplaceRow, MonetizationSummary } from "./types.ts"; import { apifyApiBase, apifyHeaders, ensureDir } from "./utils.ts"; import { outputRecord } from "./outputRecord.ts";  export async function writeOutputs(rows: MarketplaceRow[], monetizationSummary: MonetizationSummary) {   await Bun.write("output.json", JSON.stringify(rows, null, 2));   await pushRemoteApifyOutputs(rows, monetizationSummary);    const outputStoreDir = process.env.APIFY_OUTPUT_KEY_VALUE_STORE_DIR;   if (outputStoreDir) {     await ensureDir(outputStoreDir);     await Bun.write(`${outputStoreDir}/OUTPUT.json`, JSON.stringify(outputRecord(rows, monetizationSummary), null, 2));   }    const localStorageDir = process.env.APIFY_LOCAL_STORAGE_DIR;   if (localStorageDir) {     const datasetDir = `${localStorageDir}/datasets/default`;     const keyValueDir = `${localStorageDir}/key_value_stores/default`;     await ensureDir(datasetDir);     await ensureDir(keyValueDir);     await Bun.write(`${keyValueDir}/OUTPUT.json`, JSON.stringify(outputRecord(rows, monetizationSummary), null, 2));     await Promise.all(rows.map((row, index) => {       const id = String(index + 1).padStart(9, "0");       return Bun.write(`${datasetDir}/${id}.json`, JSON.stringify(row, null, 2));     }));   } }  async function pushRemoteApifyOutputs(rows: MarketplaceRow[], monetizationSummary: MonetizationSummary) {   if (!process.env.APIFY_TOKEN) return;    const datasetId = process.env.APIFY_DEFAULT_DATASET_ID;   if (datasetId && rows.length) {     for (const row of rows) {       const response = await fetch(`${apifyApiBase()}/v2/datasets/${datasetId}/items`, {         method: "POST",         headers: {           ...apifyHeaders(),           "content-type": "application/json"         },         body: JSON.stringify(row)       });       if (!response.ok) throw new Error(await apifyResponseError("Apify dataset push", response));     }   }    const storeId = process.env.APIFY_DEFAULT_KEY_VALUE_STORE_ID;   if (storeId) {     const response = await fetch(`${apifyApiBase()}/v2/key-value-stores/${storeId}/records/OUTPUT`, {       method: "PUT",       headers: {         ...apifyHeaders(),         "content-type": "application/json"       },       body: JSON.stringify(outputRecord(rows, monetizationSummary))     });     if (!response.ok) throw new Error(await apifyResponseError("Apify output record write", response));   }    if (storeId) {     const response = await fetch(`${apifyApiBase()}/v2/key-value-stores/${storeId}/records/RUN_SUMMARY`, {       method: "PUT",       headers: {         ...apifyHeaders(),         "content-type": "application/json"       },       body: JSON.stringify(outputRecord(rows, monetizationSummary))     });     if (!response.ok) throw new Error(await apifyResponseError("Apify run summary write", response));   } }  async function apifyResponseError(label: string, response: Response): Promise<string> {   const body = await response.text().catch(() => "");   return `${label} returned ${response.status}${body ? `: ${body.slice(0, 500)}` : ""}`; }
