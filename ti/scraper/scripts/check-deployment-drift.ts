import { assertLiveSearchPromotionSummary, buildLiveSearchPromotionSummary, evaluateDeploymentDrift, evaluateLiveSearchSoak } from "../src/ops/liveSearch.ts";  const queries = ["APT29", "Scattered Spider", process.env.DRIFT_RANDOM_ACTOR_QUERY ?? "Volt Typhoon"]; const publicBase = process.env.PUBLIC_TI_BASE_URL ?? "https://hanasand.com/ti"; const apiBase = process.env.PUBLIC_TI_API_BASE_URL ?? "https://api.hanasand.com/api/ti/search";  const [publicProofs, apiSearchProofs, healthEndpoints] = await Promise.all([   Promise.all(queries.map(async (query) => ({ query, ...(await fetchText(`${publicBase}?q=${encodeURIComponent(query)}`)) }))),   Promise.all(queries.map(async (query) => ({ query, ...(await fetchJsonOrText(`${apiBase}?q=${encodeURIComponent(query)}`)) }))),   Promise.all([     fetchHealth("scraper", process.env.SCRAPER_HEALTH_URL ?? "http://127.0.0.1:8097/v1/health"),     fetchHealth("api", process.env.API_HEALTH_URL ?? "https://api.hanasand.com/health"),     fetchHealth("frontend", process.env.FRONTEND_HEALTH_URL ?? "https://hanasand.com/ti")   ]) ]);  const drift = evaluateDeploymentDrift({   localSourceHash: requiredEnv("LOCAL_SOURCE_HASH"),   remoteSourceHash: requiredEnv("REMOTE_SOURCE_HASH"),   expectedComposeConfigHash: requiredEnv("EXPECTED_COMPOSE_CONFIG_HASH"),   remoteComposeConfigHash: requiredEnv("REMOTE_COMPOSE_CONFIG_HASH"),   expectedImageId: requiredEnv("EXPECTED_IMAGE_ID"),   runningImageId: requiredEnv("RUNNING_IMAGE_ID"),   healthEndpoints,   publicProofs,   apiSearchProofs,   randomActorQuery: queries[2] ?? "Volt Typhoon",   rollbackTarget: {     sourceHash: requiredEnv("LAST_KNOWN_GOOD_SOURCE_HASH"),     imageId: requiredEnv("LAST_KNOWN_GOOD_IMAGE_ID"),     composeConfigHash: requiredEnv("LAST_KNOWN_GOOD_COMPOSE_CONFIG_HASH"),     command: process.env.ROLLBACK_COMMAND ?? "docker compose up -d ti-scraper api frontend --no-build"   } }); const soak = evaluateLiveSearchSoak({   scenario: "success",   durationHours: Number(process.env.TI_SOAK_DURATION_HOURS ?? "24"),   publicProofOk: publicProofs.every((proof) => proof.status >= 200 && proof.status < 300),   scraperNativeProofOk: process.env.SCRAPER_NATIVE_PROOF_OK !== "false",   apiWrapperProofOk: apiSearchProofs.every((proof) => proof.status >= 200 && proof.status < 300),   sourceActivationDryRunOk: process.env.SOURCE_ACTIVATION_DRY_RUN_OK !== "false",   initialLatencyP95Ms: Number(process.env.TI_INITIAL_LATENCY_P95_MS ?? "0"),   partialLatencyP95Ms: Number(process.env.TI_PARTIAL_LATENCY_P95_MS ?? "0"),   errorRatePercent: Number(process.env.TI_ERROR_RATE_PERCENT ?? "0"),   duplicateActiveRuns: Number(process.env.TI_DUPLICATE_ACTIVE_RUNS ?? "0"),   sourceCoveragePercent: Number(process.env.TI_SOURCE_COVERAGE_PERCENT ?? "100"),   queueAgeP95Seconds: Number(process.env.TI_QUEUE_AGE_P95_SECONDS ?? "0"),   workerSaturationPercent: Number(process.env.TI_WORKER_SATURATION_PERCENT ?? "0"),   memoryRssMaxGb: Number(process.env.TI_MEMORY_RSS_MAX_GB ?? "0"),   policyBlocks: Number(process.env.TI_POLICY_BLOCKS ?? "0"),   policyBlockRatePercent: Number(process.env.TI_POLICY_BLOCK_RATE_PERCENT ?? "0"),   unsafePolicyRetries: Number(process.env.TI_UNSAFE_POLICY_RETRIES ?? "0"),   sourceUnavailableRatePercent: Number(process.env.TI_SOURCE_UNAVAILABLE_RATE_PERCENT ?? "0"),   staleCacheRatePercent: Number(process.env.TI_STALE_CACHE_RATE_PERCENT ?? "0"),   fallbackUsed: process.env.TI_FALLBACK_USED === "true" }); const summary = buildLiveSearchPromotionSummary(soak, drift);  for (const check of drift.checks) {   console.log(JSON.stringify({     event: "deployment_drift.check",     name: check.name,     ok: check.ok,     message: check.message   })); } console.log(JSON.stringify({   event: "deployment_drift.summary",   ok: summary.ok,   status: summary.status,   deploymentDriftState: summary.deploymentDriftState,   rollbackTarget: summary.rollbackTarget,   lastKnownGood: summary.lastKnownGood,   blockedPromotionReasons: summary.blockedPromotionReasons }));  assertLiveSearchPromotionSummary(summary);  async function fetchHealth(name: string, url: string): Promise<{ name: string; url: string; status: number; ok?: boolean; json?: unknown; body?: string }> {   const result = await fetchJsonOrText(url);   const json = typeof result.json === "object" && result.json !== null ? result.json as Record<string, unknown> : undefined;   return {     name,     ...result,     ok: json?.ok === true || json?.status === "ok" || result.body?.toLowerCase().includes("ok")   }; }  async function fetchText(url: string): Promise<{ url: string; status: number; body: string }> {   try {     const response = await fetch(url);     return { url, status: response.status, body: await response.text() };   } catch (error) {     return { url, status: 0, body: error instanceof Error ? error.message : String(error) };   } }  async function fetchJsonOrText(url: string): Promise<{ url: string; status: number; json?: unknown; body?: string }> {   const response = await fetchText(url);   try {     return { url, status: response.status, json: JSON.parse(response.body) };   } catch {     return response;   } }  function requiredEnv(name: string): string {   const value = process.env[name];   if (value && value.trim().length > 0) return value;   return `unset:${name}`; }
