# Resilience credentials

This private credential store moved here from /home/hanasand/resilience-secrets.
The old path remains a symlink for existing deployment scripts and running
containers.

This directory contains private SSH keys and runtime environment credentials.
All credential files are intentionally excluded from Git. Keep directory
permissions restricted and never force-add its contents.
