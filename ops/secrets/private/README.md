# Private credentials

Private files are provisioned under `/home/hanasand/secrets`; Git tracks only
this README and its ignore rules. Keep the directory restricted and never
force-add its contents to the project history.
