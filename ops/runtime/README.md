# Runtime data

This directory is mounted by the Inspur services at `/home/hanasand/hanasand/ops/runtime`.
It holds mutable production state, logs, backups, generated release snapshots,
and credentials. Git tracks only this README and its ignore rules. Keep all
runtime data out of the project history.
