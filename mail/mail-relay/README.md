# Mail relay runtime data

This directory contains private mail relay credentials, SSH material, and
health state used by the Hanasand mail relay service. It was moved from the
user home directory into the project. Runtime data is intentionally excluded
from Git; only this documentation and ignore file may be tracked.
