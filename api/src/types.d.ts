type User = {
    id: string
    name: string
    avatar: string
}

type Role = {
    id: number
    name: string
    description?: string
    created_by: string
    created_at: string
    updated_at: string
}
