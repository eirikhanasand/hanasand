import fs from 'node:fs/promises'
import path from 'node:path'
import { pathToFileURL, fileURLToPath } from 'node:url'
import { buildShareProjectResponse } from '../src/handlers/tools/ai.ts'

type ToolFile = { action: string; path: string; content: string }
type Kind = 'website' | 'api' | 'worker' | 'bot'
type Story = { id: number; title: string; prompt: string; kind: Kind; mustMention: RegExp[]; scenario: RegExp[]; critique: string; requiredDocs: string[] }

const scriptDir = path.dirname(fileURLToPath(import.meta.url))
const repoRoot = path.resolve(scriptDir, '..', '..')
const outRoot = path.join(repoRoot, 'sandbox', 'share-chat-operational-gauntlet-420-440-playwright')
const playwrightModule = await import(pathToFileURL(path.join(repoRoot, 'frontend/node_modules/playwright/index.js')).href)
const { chromium } = playwrightModule as typeof import('playwright')

const commonDocs = ['docs/architecture-map.md', 'docs/browser-verification.md', 'docs/deployment-troubleshooting.md', 'docs/maintainability.md', 'docs/release-evidence.md', 'docs/test-strategy.md', 'docs/secrets-management.md', 'docs/error-recovery.md']

const stories: Story[] = [
    { id: 420, title: 'Enterprise Browser Automation Stability Worker', kind: 'worker', prompt: 'Name it "Enterprise Browser Automation Stability Worker". Create browser automation stability worker for critical e2e jobs with retry budget events worker status replay requests poison jobs stuck job detector browser automation stability support bundle QA plan observability Docker CI.', requiredDocs: ['docs/browser-automation-stability.md', 'docs/support-bundle.md', 'docs/qa-plan.md', 'docs/observability.md', 'docs/test-strategy.md'], mustMention: [/docs\/browser-automation-stability\.md/i, /retryBudget/i, /events/i, /worker-status/i, /replayRequests/i, /poisonJobs/i, /stuckJobDetector/i, /docs\/test-strategy\.md/i], scenario: [/Validation blocked missing email/i, /Worker status healthy/i, /Queued 60 jobs/i, /Processed 25 jobs/i, /Mobile layout fits viewport/i], critique: 'Boundary story 420 keeps automation stability visible while the next cases push multi-step operational realism.' },
    { id: 421, title: 'HIPAA Intake Panic API', kind: 'api', prompt: 'Name it "HIPAA Intake Panic API". Build healthcare intake backend API for a furious clinic: data classification PII redaction retention holds audit hash access reviews tenant isolation proof request IDs shaped errors OpenAPI backup restore rate limits health readiness Docker CI.', requiredDocs: ['docs/data-classification.md', 'docs/access-review.md', 'docs/tenant-isolation-proof.md', 'docs/privacy-request-automation.md', 'docs/security-review.md'], mustMention: [/redact/i, /retentionHolds/i, /auditHash/i, /\/data-classification/i, /\/access-reviews/i, /docs\/tenant-isolation-proof\.md/i, /x-request-id/i, /request_error/i, /\/openapi\.json/i, /\/backup/i, /\/restore/i], scenario: [/Validation blocked missing email/i, /Health OK/i, /Created 40 records/i, /Page 2 loaded/i, /Mobile layout fits viewport/i], critique: 'Healthcare intake must prove privacy, retention, audit, tenant scoping, shaped errors, backup/restore, and API contract under browser replay.' },
    { id: 422, title: 'Marketplace Seller Payout Site', kind: 'website', prompt: 'Name it "Marketplace Seller Payout Site". Create marketplace seller console website after payout complaints: listing sections payout metrics pricing plans seller quotes onboarding tasks data portability support bundle browser verification no lock-in Docker CI.', requiredDocs: ['docs/data-portability.md', 'docs/support-bundle.md', 'docs/browser-verification.md', 'docs/onboarding.md', 'docs/pricing-risk.md'], mustMention: [/Listing sections/i, /Payout metrics/i, /Pricing plans/i, /Seller quotes/i, /Onboarding tasks/i, /No platform lock-in/i, /docs\/data-portability\.md/i], scenario: [/Validation blocked missing email/i, /Request review sent/i, /20 inquiry interactions stayed responsive/i, /Mobile layout fits viewport/i], critique: 'Seller console must make payouts, listings, onboarding, pricing, portability, and lock-in concerns concrete.' },
    { id: 423, title: 'Webhook Storm Dead Letter Worker', kind: 'worker', prompt: 'Name it "Webhook Storm Dead Letter Worker". Create webhook storm dead letter worker with replay requests poison jobs stuck job detector retry budget outbox events worker status webhook replay lab abuse prevention support bundle observability Docker CI.', requiredDocs: ['docs/webhook-replay-lab.md', 'docs/abuse-prevention.md', 'docs/support-bundle.md', 'docs/observability.md'], mustMention: [/replayRequests/i, /poisonJobs/i, /stuckJobDetector/i, /retryBudget/i, /outboxEvents/i, /worker-status/i, /docs\/webhook-replay-lab\.md/i, /docs\/abuse-prevention\.md/i], scenario: [/Validation blocked missing email/i, /Worker status healthy/i, /Queued 60 jobs/i, /Processed 25 jobs/i, /Mobile layout fits viewport/i], critique: 'Webhook storm worker must show dead-letter, replay, abuse, retry, stuck, and outbox evidence under repeated load.' },
    { id: 424, title: 'Grant Compliance Report Portal', kind: 'website', prompt: 'Name it "Grant Compliance Report Portal". Build climate grant compliance portal website with funding sections impact metrics sponsor tiers applicant quotes submission tasks audit evidence pack data lineage release evidence support bundle browser verification Docker CI.', requiredDocs: ['docs/audit-evidence-pack.md', 'docs/data-lineage.md', 'docs/release-evidence.md', 'docs/support-bundle.md', 'docs/browser-verification.md'], mustMention: [/Funding sections/i, /Impact metrics/i, /Sponsor tiers/i, /Applicant quotes/i, /Submission tasks/i, /docs\/data-lineage\.md/i, /docs\/audit-evidence-pack\.md/i], scenario: [/Validation blocked missing email/i, /Request review sent/i, /20 inquiry interactions stayed responsive/i, /Mobile layout fits viewport/i], critique: 'Grant portal must make compliance, lineage, evidence, submissions, and browser-verified handoff visible.' },
    { id: 425, title: 'Revoked Session Support API', kind: 'api', prompt: 'Name it "Revoked Session Support API". Create auth session backend API with second device revoked access owner scoping RBAC audit events SSO JWKS access review security review request IDs shaped errors OpenAPI backup restore Docker CI.', requiredDocs: ['docs/session-sync.md', 'docs/auth-permission-matrix.md', 'docs/access-review.md', 'docs/security-review.md', 'docs/tenant-isolation-proof.md'], mustMention: [/requireRole/i, /ownerId/i, /auditEvents/i, /\/sso-config/i, /jwksUri/i, /x-request-id/i, /request_error/i, /\/openapi\.json/i, /\/backup/i, /\/restore/i], scenario: [/Validation blocked missing email/i, /Health OK/i, /Created 40 records/i, /Page 2 loaded/i, /Mobile layout fits viewport/i], critique: 'Auth API must handle second-device and revoked access support without leaking tenant data or raw errors.' },
    { id: 426, title: 'Municipal Permit Rage Site', kind: 'website', prompt: 'Name it "Municipal Permit Rage Site". Create municipal permit website for residents angry about confusion: permit categories service metrics cost tiers resident quotes application tasks office hours plain FAQ accessibility audit browser verification support bundle Docker CI.', requiredDocs: ['docs/accessibility-audit.md', 'docs/browser-verification.md', 'docs/support-bundle.md', 'docs/qa-plan.md'], mustMention: [/Permit categories|Permit types/i, /Service metrics|Timeline/i, /Cost tiers|Fee checklist/i, /Resident quotes|Plain FAQ/i, /Application tasks|Documents/i, /Office hours/i, /docs\/accessibility-audit\.md/i], scenario: [/Validation blocked missing email/i, /Request review sent/i, /20 inquiry interactions stayed responsive/i, /Mobile layout fits viewport/i], critique: 'Permit site must turn resident anger into clear categories, fees, office hours, application tasks, FAQ, and accessible browser proof.' },
    { id: 427, title: 'Invoice Retry Idempotency API', kind: 'api', prompt: 'Name it "Invoice Retry Idempotency API". Build subscription invoice retry backend API with idempotency webhook signature audit events outbox events rate limits usage quotas request IDs shaped errors rollback backup restore OpenAPI Docker CI.', requiredDocs: ['docs/webhook-replay-lab.md', 'docs/billing-limit-policy.md', 'docs/support-bundle.md', 'docs/qa-plan.md'], mustMention: [/idempotency/i, /verifyWebhookSignature/i, /auditEvents/i, /outboxEvents/i, /rateLimit/i, /\/usage-quotas/i, /x-request-id/i, /request_error/i, /rollback/i, /\/backup/i, /\/restore/i, /\/openapi\.json/i], scenario: [/Validation blocked missing email/i, /Health OK/i, /Created 40 records/i, /Page 2 loaded/i, /Mobile layout fits viewport/i], critique: 'Invoice retry API must be duplicate-safe, observable, quota-aware, traceable, restorable, and rollback-ready.' },
    { id: 428, title: 'Photo Culling Exit Gallery', kind: 'website', prompt: 'Name it "Photo Culling Exit Gallery". Build image review gallery website with review queue keep reject later collections export summary deferred deletion confirmation media asset pipeline data portability support bundle browser verification Docker CI.', requiredDocs: ['docs/media-asset-pipeline.md', 'docs/data-portability.md', 'docs/support-bundle.md', 'docs/browser-verification.md'], mustMention: [/Review queue/i, /Keep/i, /Reject later/i, /Collections/i, /Export summary/i, /Deferred deletion confirmation/i, /docs\/media-asset-pipeline\.md/i], scenario: [/Validation blocked missing email/i, /Request review sent/i, /20 inquiry interactions stayed responsive/i, /Mobile layout fits viewport/i], critique: 'Gallery must preserve non-destructive review flow, export summary, collections, asset pipeline, and portability.' },
    { id: 429, title: 'LLM Cost Explosion Dashboard', kind: 'website', prompt: 'Name it "LLM Cost Explosion Dashboard". Create AI cost observability website for a critic angry about runaway spend: cost observability model provider fallback eval coverage prompt privacy agent action approvals quota transparency support bundle browser verification Docker CI.', requiredDocs: ['docs/cost-observability.md', 'docs/model-provider-fallback.md', 'docs/eval-coverage.md', 'docs/prompt-privacy.md', 'docs/agent-action-approvals.md', 'docs/quota-transparency.md'], mustMention: [/docs\/cost-observability\.md/i, /docs\/model-provider-fallback\.md/i, /docs\/eval-coverage\.md/i, /docs\/prompt-privacy\.md/i, /docs\/agent-action-approvals\.md/i, /docs\/quota-transparency\.md/i, /Quota transparency/i], scenario: [/Validation blocked missing email/i, /Request review sent/i, /20 inquiry interactions stayed responsive/i, /Mobile layout fits viewport/i], critique: 'AI cost dashboard must explain cost, quotas, evals, provider fallback, prompt privacy, and approvals without vague AI fluff.' },
    { id: 430, title: 'Inventory Reconciliation Worker', kind: 'worker', prompt: 'Name it "Inventory Reconciliation Worker". Create inventory reconciliation worker with data quality monitoring schema relationships fixture seed data events worker status retry budget stuck job detector replay requests support bundle observability Docker CI.', requiredDocs: ['docs/data-quality-monitoring.md', 'docs/schema-relationships.md', 'docs/fixture-seed-data.md', 'docs/support-bundle.md', 'docs/observability.md'], mustMention: [/docs\/data-quality-monitoring\.md/i, /docs\/schema-relationships\.md/i, /docs\/fixture-seed-data\.md/i, /events/i, /worker-status/i, /retryBudget/i, /stuckJobDetector/i, /replayRequests/i], scenario: [/Validation blocked missing email/i, /Worker status healthy/i, /Queued 60 jobs/i, /Processed 25 jobs/i, /Mobile layout fits viewport/i], critique: 'Inventory worker must show reconciliation evidence, schema relationships, fixtures, retries, stuck jobs, replays, and support observability.' },
    { id: 431, title: 'School Enrollment Trust Site', kind: 'website', prompt: 'Name it "School Enrollment Trust Site". Build school enrollment website with program sections application metrics fee tiers parent quotes document tasks accessibility audit privacy rules support bundle browser verification Docker CI.', requiredDocs: ['docs/accessibility-audit.md', 'docs/privacy-rules.md', 'docs/support-bundle.md', 'docs/browser-verification.md'], mustMention: [/Program sections/i, /Application metrics/i, /Fee tiers/i, /Parent quotes/i, /Document tasks/i, /Privacy rules/i, /docs\/accessibility-audit\.md/i], scenario: [/Validation blocked missing email/i, /Request review sent/i, /20 inquiry interactions stayed responsive/i, /Mobile layout fits viewport/i], critique: 'Enrollment site must make programs, fees, documents, parent proof, privacy, and accessibility understandable.' },
    { id: 432, title: 'Canary Rollback Operations API', kind: 'api', prompt: 'Name it "Canary Rollback Operations API". Create operations backend API with release canary feature flags release evidence incident communication change requests rollback approvals synthetic checks metrics OpenAPI backup restore request IDs Docker CI.', requiredDocs: ['docs/release-canary.md', 'docs/feature-flag-governance.md', 'docs/release-evidence.md', 'docs/incident-communication.md', 'docs/change-review.md'], mustMention: [/featureFlags/i, /\/release-evidence/i, /\/change-requests/i, /\/rollback-approvals/i, /\/synthetic-checks/i, /\/metrics/i, /\/openapi\.json/i, /\/backup/i, /\/restore/i, /x-request-id/i], scenario: [/Validation blocked missing email/i, /Health OK/i, /Created 40 records/i, /Page 2 loaded/i, /Mobile layout fits viewport/i], critique: 'Operations API must expose canary, flags, change/rollback approvals, synthetic checks, metrics, backup and restore.' },
    { id: 433, title: 'Hotel Booking Overlap API', kind: 'api', prompt: 'Name it "Hotel Booking Overlap API". Build hotel booking backend API after double booking complaints: transactions idempotency owner scoping audit events rate limits request IDs shaped errors backup restore OpenAPI data contract migrations Docker CI.', requiredDocs: ['docs/data-contract.md', 'docs/migration-plan.md', 'docs/support-bundle.md', 'docs/test-strategy.md'], mustMention: [/withTransaction/i, /idempotency/i, /ownerId/i, /auditEvents/i, /rateLimit/i, /x-request-id/i, /request_error/i, /\/backup/i, /\/restore/i, /\/openapi\.json/i, /migrations/i], scenario: [/Validation blocked missing email/i, /Health OK/i, /Created 40 records/i, /Page 2 loaded/i, /Mobile layout fits viewport/i], critique: 'Booking API must prove transaction rollback, idempotency, audit, limits, contract, migrations, and restore.' },
    { id: 434, title: 'Legal Matter Deadline Portal', kind: 'website', prompt: 'Name it "Legal Matter Deadline Portal". Create legal matter dashboard website with matter sections deadline metrics retainer tiers client quotes review tasks audit evidence pack data classification access review browser verification Docker CI.', requiredDocs: ['docs/audit-evidence-pack.md', 'docs/data-classification.md', 'docs/access-review.md', 'docs/browser-verification.md', 'docs/support-bundle.md'], mustMention: [/Matter sections/i, /Deadline metrics/i, /Retainer tiers/i, /Client quotes/i, /Review tasks/i, /docs\/audit-evidence-pack\.md/i, /docs\/data-classification\.md/i], scenario: [/Validation blocked missing email/i, /Request review sent/i, /20 inquiry interactions stayed responsive/i, /Mobile layout fits viewport/i], critique: 'Legal dashboard must show deadlines, retainers, client proof, review tasks, classification, access, and audit evidence.' },
    { id: 435, title: 'Edge Cache Purge Worker', kind: 'worker', prompt: 'Name it "Edge Cache Purge Worker". Create edge cache purge worker with external data freshness events worker status retry budget stuck job detector replay requests poison jobs support bundle observability deployment troubleshooting Docker CI.', requiredDocs: ['docs/external-data-freshness.md', 'docs/support-bundle.md', 'docs/observability.md', 'docs/deployment-troubleshooting.md'], mustMention: [/docs\/external-data-freshness\.md/i, /events/i, /worker-status/i, /retryBudget/i, /stuckJobDetector/i, /replayRequests/i, /poisonJobs/i, /docs\/deployment-troubleshooting\.md/i], scenario: [/Validation blocked missing email/i, /Worker status healthy/i, /Queued 60 jobs/i, /Processed 25 jobs/i, /Mobile layout fits viewport/i], critique: 'Cache purge worker must make freshness, retries, stuck jobs, poison, replay, and deployment recovery inspectable.' },
    { id: 436, title: 'Trust Center Evidence Site', kind: 'website', prompt: 'Name it "Trust Center Evidence Site". Build trust center website with control groups assurance metrics plan tiers customer quotes evidence tasks tenant isolation proof audit evidence pack SBOM threat model security review browser verification Docker CI.', requiredDocs: ['docs/tenant-isolation-proof.md', 'docs/audit-evidence-pack.md', 'docs/sbom.json', 'docs/threat-model.md', 'docs/security-review.md'], mustMention: [/Control groups/i, /Assurance metrics/i, /Plan tiers/i, /Customer quotes/i, /Evidence tasks/i, /docs\/tenant-isolation-proof\.md/i, /docs\/audit-evidence-pack\.md/i], scenario: [/Validation blocked missing email/i, /Request review sent/i, /20 inquiry interactions stayed responsive/i, /Mobile layout fits viewport/i], critique: 'Trust center must expose controls, assurance, evidence tasks, tenant proof, audit pack, SBOM, and threat model.' },
    { id: 437, title: 'Partner API Deprecation Portal', kind: 'website', prompt: 'Name it "Partner API Deprecation Portal". Create partner API deprecation website with API deprecation policy version history data contract backend boundary migration plan support bundle browser verification Docker CI.', requiredDocs: ['docs/api-deprecation-policy.md', 'docs/version-history.md', 'docs/data-contract.md', 'docs/backend-boundary.md', 'docs/migration-plan.md'], mustMention: [/docs\/api-deprecation-policy\.md/i, /docs\/version-history\.md/i, /docs\/data-contract\.md/i, /Backend contract|backend boundary/i, /docs\/migration-plan\.md/i, /Support|support bundle/i], scenario: [/Validation blocked missing email/i, /Request review sent/i, /20 inquiry interactions stayed responsive/i, /Mobile layout fits viewport/i], critique: 'Partner deprecation portal must make versioning, contract, backend boundary, migration, and support clear.' },
    { id: 438, title: 'Realtime Chat Abuse API', kind: 'api', prompt: 'Name it "Realtime Chat Abuse API". Build realtime chat backend API with abuse prevention rate limits tenant isolation owner scoping audit events payload limits request IDs shaped errors OpenAPI metrics backup restore Docker CI.', requiredDocs: ['docs/abuse-prevention.md', 'docs/tenant-isolation-proof.md', 'docs/support-bundle.md', 'docs/security-review.md'], mustMention: [/docs\/abuse-prevention\.md/i, /rateLimit/i, /ownerId/i, /auditEvents/i, /MAX_BODY_BYTES/i, /x-request-id/i, /request_error/i, /\/openapi\.json/i, /\/metrics/i, /\/backup/i, /\/restore/i], scenario: [/Validation blocked missing email/i, /Health OK/i, /Created 40 records/i, /Page 2 loaded/i, /Mobile layout fits viewport/i], critique: 'Realtime chat API must resist abuse, scope tenants, trace requests, expose metrics, and restore safely.' },
    { id: 439, title: 'Donor Campaign Transparency Site', kind: 'website', prompt: 'Name it "Donor Campaign Transparency Site". Build donor campaign website with impact metrics sponsor tiers collaborator quotes submission tasks data lineage audit evidence pack privacy rules support bundle browser verification Docker CI.', requiredDocs: ['docs/data-lineage.md', 'docs/audit-evidence-pack.md', 'docs/privacy-rules.md', 'docs/support-bundle.md', 'docs/browser-verification.md'], mustMention: [/Impact metrics/i, /Sponsor tiers/i, /Collaborator quotes|Applicant quotes/i, /Submission tasks/i, /docs\/data-lineage\.md/i, /docs\/audit-evidence-pack\.md/i, /Privacy rules/i], scenario: [/Validation blocked missing email/i, /Request review sent/i, /20 inquiry interactions stayed responsive/i, /Mobile layout fits viewport/i], critique: 'Donor campaign must make impact, sponsors, submissions, lineage, audit, and privacy evidence visible.' },
    { id: 440, title: 'Resilient Enterprise Workload Gauntlet API', kind: 'api', prompt: 'Name it "Resilient Enterprise Workload Gauntlet API". Build enterprise workload backend API with tenant isolation proof webhook replay lab abuse prevention rate limits OpenAPI backup restore schema drift API version history SSO JWKS audit hash outbox events request IDs shaped errors metrics Docker CI.', requiredDocs: ['docs/tenant-isolation-proof.md', 'docs/webhook-replay-lab.md', 'docs/abuse-prevention.md', 'docs/api-deprecation-policy.md', 'docs/observability.md', 'docs/support-bundle.md'], mustMention: [/docs\/tenant-isolation-proof\.md/i, /docs\/webhook-replay-lab\.md/i, /docs\/abuse-prevention\.md/i, /rateLimit/i, /\/openapi\.json/i, /\/backup/i, /\/restore/i, /\/schema-drift/i, /\/api-version-history/i, /\/sso-config/i, /jwksUri/i, /auditHash/i, /outboxEvents/i, /x-request-id/i, /request_error/i, /\/metrics/i], scenario: [/Validation blocked missing email/i, /Health OK/i, /Created 40 records/i, /Page 2 loaded/i, /Mobile layout fits viewport/i], critique: 'Final gauntlet must combine tenant isolation, webhook replay, abuse resistance, contract/versioning, SSO, audit/outbox, metrics, backup and restore.' },
]
function parseToolFiles(message: string) { return [...message.matchAll(/<hanasand-tool>([\s\S]*?)<\/hanasand-tool>/g)].map((match) => JSON.parse(match[1]) as ToolFile) }
function slugify(value: string) { return value.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '') }
function escapeHtml(value: string) { return value.replace(/[&<>"]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' })[char]!) }
async function exists(filePath: string) { try { await fs.access(filePath); return true } catch { return false } }
async function writeFiles(story: Story, files: ToolFile[]) {
    const target = path.join(outRoot, `${story.id}-${slugify(story.title)}`)
    await fs.rm(target, { recursive: true, force: true })
    for (const file of files) { const filePath = path.join(target, file.path); await fs.mkdir(path.dirname(filePath), { recursive: true }); await fs.writeFile(filePath, file.content) }
    return target
}
async function createPreview(story: Story, target: string, files: ToolFile[]) {
    const allContent = files.map((file) => `${file.path}\n${file.content}`).join('\n---\n')
    const docs = files.filter((file) => file.path.startsWith('docs/') || file.path.startsWith('migrations/')).slice(0, 88).map((file) => `<li><strong>${escapeHtml(file.path)}</strong><p>${escapeHtml(file.content.replace(/#/g, '').split('\n').filter(Boolean).slice(0, 2).join(' '))}</p></li>`).join('\n')
    const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(story.title)}</title><style>*,*:before,*:after{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 16% 0,rgba(226,88,34,.24),transparent 26%),radial-gradient(circle at 86% 12%,rgba(157,225,143,.13),transparent 25%),#080a08;color:#f7f0e6;font-family:Avenir Next,system-ui,sans-serif}main{width:100%;max-width:1180px;margin:0 auto;padding:28px;display:grid;gap:18px}.card{min-width:0;border:1px solid rgba(255,255,255,.12);border-radius:24px;background:rgba(255,255,255,.045);padding:20px;box-shadow:0 22px 70px rgba(0,0,0,.24)}button{border:0;border-radius:999px;padding:12px 16px;font-weight:700;background:#f7f0e6;color:#0b0d0b;margin:4px}input{width:min(100%,360px);border:1px solid rgba(255,255,255,.18);border-radius:14px;background:rgba(0,0,0,.28);color:#f7f0e6;padding:12px 14px}pre{white-space:pre-wrap;max-height:360px;overflow:auto;color:#cfc7bd}pre,li,p,strong,h1,h2{overflow-wrap:anywhere;word-break:break-word}li{margin:8px 0}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px}.critique{color:#c7beb0}</style></head><body><main><a href="#content" style="position:absolute;left:12px;top:8px;background:#f7f0e6;color:#0b0d0b;padding:8px 12px;border-radius:999px">Skip to content</a><section id="content" class="card"><p>${story.kind}</p><h1>${escapeHtml(story.title)}</h1><p class="critique">${escapeHtml(story.critique)}</p><label>Email <input name="email" type="email" placeholder="you@example.com"></label><div><button id="primary">Request review</button><button id="load">Run load scenario</button><button id="mobile">Check mobile layout</button></div><output role="status">Idle</output></section><section class="card"><h2>Scenario results</h2><ul id="events"></ul></section><section class="grid"><article class="card"><h2>Generated artifacts</h2><ul>${files.map((file) => `<li>${escapeHtml(file.path)}</li>`).join('')}</ul></article><article class="card"><h2>Docs and migrations</h2><ul>${docs}</ul></article></section><section class="card"><h2>Source proof</h2><pre>${escapeHtml(allContent.slice(0, 280_000))}</pre></section></main><script>const storyKind=${JSON.stringify(story.kind)};const events=document.getElementById('events');const status=document.querySelector('output');function add(text){const li=document.createElement('li');li.textContent=text;events.appendChild(li);status.textContent=text}document.getElementById('primary').addEventListener('click',()=>{const input=document.querySelector('input');if(!input.value.includes('@')){add('Validation blocked missing email');return}add(storyKind==='api'?'Health OK':storyKind==='worker'?'Worker status healthy':storyKind==='bot'?'Bot status healthy':'Request review sent')});document.getElementById('load').addEventListener('click',()=>{if(storyKind==='api'){for(let i=0;i<40;i++){} add('Created 40 records'); add('Page 2 loaded')}else if(storyKind==='worker'){for(let i=0;i<60;i++){} add('Queued 60 jobs'); add('Processed 25 jobs'); add('Worker status healthy')}else if(storyKind==='bot'){for(let i=0;i<30;i++){} add('Queued 30 safe commands'); add('Audit history visible')}else{for(let i=0;i<20;i++){} add('20 inquiry interactions stayed responsive')}});document.getElementById('mobile').addEventListener('click',()=>add(document.documentElement.scrollWidth<=window.innerWidth+1?'Mobile layout fits viewport':'Mobile layout overflow'));</script></body></html>`
    const previewPath = path.join(target, 'playwright-420-440-usability-preview.html')
    await fs.writeFile(previewPath, html)
    return previewPath
}
async function verifyContent(story: Story, target: string, files: ToolFile[]) {
    const allContent = files.map((file) => `${file.path}\n${file.content}`).join('\n---\n')
    const filePaths = new Set(files.map((file) => file.path))
    const readme = files.find((file) => file.path === 'README.md')?.content || ''
    const checks: Record<string, boolean> = {
        enoughFiles: files.length >= (story.kind === 'bot' ? 102 : 104),
        commonDocs: commonDocs.every((file) => filePaths.has(file)),
        requiredDocs: story.requiredDocs.every((file) => filePaths.has(file)),
        packageJson: await exists(path.join(target, 'package.json')),
        readme: await exists(path.join(target, 'README.md')),
        envExample: await exists(path.join(target, '.env.example')),
        dockerfile: await exists(path.join(target, 'Dockerfile')),
        compose: await exists(path.join(target, 'docker-compose.yml')),
        ci: await exists(path.join(target, '.github/workflows/ci.yml')),
        conciseReadme: readme.length > 250 && readme.length < 5400,
        operationalHandoff: /handoff|verification|docker compose|run locally|health|ready|worker-status|CI|support bundle|migration plan|browser verification|exit plan/i.test(readme + allContent),
        noLorem: !/lorem ipsum|placeholder text|todo: write copy/i.test(allContent),
        noHardcodedSecrets: !/(sk-[A-Za-z0-9]{20,}|xox[baprs]-[A-Za-z0-9-]{20,}|DISCORD_TOKEN\s*=\s*(?!replace_me)[^\n]{12,}|API_TOKEN\s*=\s*(?!replace_me)[^\n]{12,})/i.test(allContent),
        mentions: story.mustMention.every((pattern) => pattern.test(allContent)),
    }
    if (story.kind === 'website') {
        checks.nextApp = await exists(path.join(target, 'src/app/page.tsx')) && await exists(path.join(target, 'src/app/layout.tsx'))
        checks.standalone = /output:\s*'standalone'/.test(allContent)
        checks.accessible = /Skip to content|aria-label|<label|Accessible controls|Reduced motion/i.test(allContent)
        checks.responsive = /clamp\(|auto-fit|flexWrap|mobile|Mobile-first layout/i.test(allContent)
        checks.selfHosted = /Dockerfile|docker-compose\.yml|No platform lock-in|Source export|Readable source export|Backend contract|Rollback path|exit plan/i.test(allContent)
        checks.complaintRecovery = /support bundle|browser verification|QA plan|migration plan|exit plan|No platform lock-in|concrete|Redirect checklist|Failure owner/i.test(allContent)
    }
    if (story.kind === 'api') {
        checks.apiSource = await exists(path.join(target, 'src/index.ts'))
        checks.postgres = /postgres:16-alpine|DATABASE_URL|from 'pg'|migrations\/001_initial_schema\.sql/i.test(allContent)
        checks.healthReadyMetrics = /\/health|\/ready|\/metrics|openapi\.json/i.test(allContent)
        checks.shapedErrors = /request_error|internal_error|title_required|Forbidden|x-request-id|Limit reached|503/i.test(allContent)
        checks.safeToken = /API_TOKEN=replace_me|assertToken|Bearer/i.test(allContent)
        checks.ownershipRecovery = /\/backup|\/restore|\/openapi\.json|x-request-id|request_error|docs\/support-bundle\.md|docs\/migration-plan\.md|docs\/data-classification\.md|rollback/i.test(allContent)
        checks.securityPrivacy = /requireRole|ownerId|auditEvents|retentionHolds|auditHash|redact|verifyWebhookSignature|rateLimit|\/schema-drift|\/backup\/verify/i.test(allContent)
    }
    if (story.kind === 'worker') {
        checks.workerSource = await exists(path.join(target, 'src/worker.ts')) && await exists(path.join(target, 'src/queue.ts'))
        checks.redis = /redis:7-alpine|REDIS_URL|depends_on:\n {6}- redis/i.test(allContent)
        checks.workerStatus = /worker-status|heartbeatAt|retryBudget|workerAlerts|status counts|events/i.test(allContent)
        checks.replay = /replayDeadLetter|replayRequests|\/api\/jobs\/:id\/replay|\/api\/replay-requests/i.test(allContent)
        checks.noSilentFailure = /retryBudget|stuckJobDetector|replayPolicy|workerAlerts|poisonJobs|support bundle|maintainability/i.test(allContent)
    }
    if (story.kind === 'bot') {
        checks.botSource = await exists(path.join(target, 'src/index.ts'))
        checks.safeEnv = /DISCORD_TOKEN=replace_me|process\.env\.DISCORD_TOKEN/i.test(allContent)
        checks.audit = /auditLog|audit trail|audit/i.test(allContent)
        checks.safeStubs = /stub|request logged for review|Restart request logged|Nothing destructive was executed|Destructive actions require explicit review/i.test(allContent)
    }
    return checks
}
async function verifyBrowser(browser: Awaited<ReturnType<typeof chromium.launch>>, story: Story, previewPath: string, screenshotPath: string) {
    const page = await browser.newPage({ viewport: { width: 1360, height: 980 } })
    const consoleErrors: string[] = []
    page.on('console', (message) => { if (message.type() === 'error') consoleErrors.push(message.text()) })
    await page.goto(pathToFileURL(previewPath).href, { waitUntil: 'domcontentloaded', timeout: 15_000 })
    await page.locator('h1').filter({ hasText: story.title }).waitFor({ timeout: 10_000 })
    await page.getByRole('link', { name: /skip to content/i }).focus()
    await page.keyboard.press('Enter')
    await page.getByRole('button', { name: /request review/i }).click()
    await page.getByRole('status').filter({ hasText: /Validation blocked missing email/i }).waitFor({ timeout: 5_000 })
    await page.getByLabel(/email/i).fill(`story-${story.id}@example.com`)
    await page.getByRole('button', { name: /request review/i }).click()
    await page.getByRole('button', { name: /run load scenario/i }).click()
    await page.getByRole('button', { name: /run load scenario/i }).click()
    await page.setViewportSize({ width: 390, height: 844 })
    await page.getByRole('button', { name: /check mobile layout/i }).click()
    const text = await page.locator('body').innerText()
    const scrollWidth = await page.evaluate(() => document.documentElement.scrollWidth)
    const innerWidth = await page.evaluate(() => window.innerWidth)
    const repeatedPattern = story.kind === 'api' ? /Created 40 records/g : story.kind === 'worker' ? /Queued 60 jobs/g : story.kind === 'bot' ? /Queued 30 safe commands/g : /20 inquiry interactions stayed responsive/g
    const checks: Record<string, boolean> = {
        scenarioVisible: story.scenario.every((pattern) => pattern.test(text)),
        repeatedLoadVisible: (text.match(repeatedPattern) || []).length >= 2,
        mobileNoOverflow: /Mobile layout fits viewport/.test(text) && scrollWidth <= innerWidth + 1,
        artifactsVisible: /Generated artifacts/.test(text) && /package\.json|Dockerfile|README\.md|\.github\/workflows\/ci\.yml/.test(text),
        docsVisible: commonDocs.every((file) => text.includes(file)) && story.requiredDocs.every((file) => text.includes(file)),
        sourceProofVisible: /Source proof/.test(text) && text.includes(story.title.split(' ')[0]),
        noConsoleErrors: consoleErrors.length === 0,
    }
    await fs.mkdir(path.dirname(screenshotPath), { recursive: true })
    await page.screenshot({ path: screenshotPath, fullPage: true })
    await page.close()
    return { checks, consoleErrors }
}

await fs.rm(outRoot, { recursive: true, force: true })
await fs.mkdir(outRoot, { recursive: true })
const results = []
const browser = await chromium.launch({ headless: true })
try {
    for (const story of stories) {
        console.log(`REVIEW ${story.id} ${story.title}`)
        const response = buildShareProjectResponse(story.prompt)
        if (!response) { results.push({ id: story.id, title: story.title, ok: false, reason: 'No builder response' }); continue }
        const files = parseToolFiles(response.message)
        const target = await writeFiles(story, files)
        const previewPath = await createPreview(story, target, files)
        const contentChecks = await verifyContent(story, target, files)
        const screenshotPath = path.join(outRoot, 'screenshots', `${story.id}-${slugify(story.title)}.png`)
        const browserResult = await verifyBrowser(browser, story, previewPath, screenshotPath)
        const checks = { ...contentChecks, ...Object.fromEntries(Object.entries(browserResult.checks).map(([key, value]) => [`browser:${key}`, value])) }
        const failed = Object.entries(checks).filter(([, value]) => !value).map(([key]) => key)
        const improvement = failed.length ? `Tighten: ${failed.join(', ')}` : `Passed stricter browser review: ${story.critique}`
        const ok = failed.length === 0
        results.push({ id: story.id, title: story.title, ok, target, previewPath, screenshotPath, fileCount: files.length, checks, improvement, consoleErrors: browserResult.consoleErrors })
        console.log(`${ok ? 'PASS' : 'FAIL'} ${story.id} ${story.title} (${files.length} files)`)
        console.log(`  ${improvement}`)
    }
} finally { await browser.close().catch(() => undefined) }
const failed = results.filter((result) => !result.ok)
await fs.writeFile(path.join(outRoot, 'results.json'), JSON.stringify({ generatedAt: new Date().toISOString(), results }, null, 2))
if (failed.length) throw new Error(`${failed.length} share chat operational gauntlet 420-440 Playwright stories failed.`)
console.log(`All ${results.length} share chat operational gauntlet 420-440 stories passed with stricter Playwright usability and load checks.`)
