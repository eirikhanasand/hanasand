location ^~ /api/auth/ {
    proxy_pass http://hanasand_auth;
    include snippets/auth-proxy.conf;
}
location = /api/user {
    proxy_pass http://hanasand_auth;
    include snippets/auth-proxy.conf;
}
